
\begin{prooftree}
  \AxiomC{\ensuremath{\codenames{success}\ t }}
  \AxiomC{\ensuremath{\codenames{next\_subst}\ \sigma\ t \arr \sigma' }}
  \AxiomC{\ensuremath{\codenames{prune}\ \top\ t \arr t' }}
  \RightLabel{\rulelabel{\stopt}}
  \TrinaryInfC{\ensuremath{\runT\ \prog\ v\ \sigma\ t \arr (\msome(\sigma', t'), \bot, v)}}
\end{prooftree}
\begin{prooftree}
  \AxiomC{\ensuremath{
      \codenames{incomplete}\ t \quad 
      \codenames{step}\ \prog\ v\ \sigma\ t \arr (v', tg, t') \quad 
      \runT\ \prog\ v'\ \sigma\ t' \arr (r, b, v'')}}
  \RightLabel{\rulelabel{\stept}}
  \UnaryInfC{\ensuremath{\runT\ \prog\ v\ \sigma\ t\ \arr (r, (tg = \codenames{CutBrothers}) \lor b, v'')}}
\end{prooftree}
\begin{prooftree}
  \AxiomC{\ensuremath{\codenames{failed}\ t} }
  \AxiomC{\ensuremath{\codenames{prune}\ \bot\ t \arr \msome t'} }
  \AxiomC{\ensuremath{\runT\ \prog\ v\ \sigma\ t' \arr r} }
  \RightLabel{\rulelabel{\backt}}
  \TrinaryInfC{\ensuremath{\runT\ \prog\ v\ \sigma\ t \arr r}}
\end{prooftree}
\begin{prooftree}
  \AxiomC{\ensuremath{\codenames{prune}\ \bot\ t \arr \square}}
  \RightLabel{\rulelabel{\failt}}
  \UnaryInfC{\ensuremath{\runT\ \prog\ v\ \sigma\ t \arr (\square, \bot, v)}}
\end{prooftree}