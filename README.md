Generated pdf file is [here](https://github.com/FissoreD/paper-elpi-semantics/blob/pdf/main.pdf)


## Tex compilation

The pdf code can be built using the `make all` command.

Alternatively, a compilation via docker is possible via the command `make ci`.
The compiled pdf file is in the `pdf` folder.