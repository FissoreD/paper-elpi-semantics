
\PassOptionsToPackage{dvipsnames,table}{xcolor}
\documentclass[sigplan,10pt,anonymous,review]{acmart}
\settopmatter{printfolios=true,printccs=false,printacmref=false}

% \usepackage{caption}
\usepackage{subcaption}
\DeclareCaptionLabelFormat{xxbracket}{Figure 6#2.}

\usepackage{comment}
\usepackage{pifont}

\hypersetup{hidelinks}

\usepackage{minted}
\usepackage{mathtools}
\usepackage{stackengine} % For bussproof multiline
\usepackage{wrapfig}
\usepackage{tikz}
\usetikzlibrary{tikzmark}
\usetikzlibrary{matrix,calc,positioning}

\usepackage{cleveref}
\usepackage{macro}
\usepackage{tree}
\usepackage{custom_ref}

\renewcommand{\epsilon}{\varepsilon}
\newcommand{\msome}{\ensuremath{\cdot}}
\newcommand{\mnone}{\ensuremath{\square}\xspace}
\newcommand{\mappend}{\ensuremath{\mathbin{+\!\!+}}\xspace}
\newcommand{\mnil}{\ensuremath{[]}\xspace}
\newtheorem{axiom}{Axiom}
\newcommand{\mphant}{\phantom{x}}

\newcommand{\logo}{{\raisebox{-0.3em}{\includegraphics[height=1em]{rocq-logo.png}}\hspace{.2em}}}

\Crefname{figure}{Fig.}{Figs.}
\usepackage{amsthm}
\usepackage{thmtools}

%\pdfoutput=1 %uncomment to ensure pdflatex processing (mandatatory e.g. to submit to arXiv)
%\hideLIPIcs  %uncomment to remove references to LIPIcs series (logo, DOI, ...), e.g. when preparing a pre-final version to be uploaded to arXiv or another public repository

%\graphicspath{{./graphics/}}%helpful if your graphic files are in another directory

% \bibliographystyle{plainurl}% the mandatory bibstyle

%\titlerunning{Dummy short title} %TODO optional, please use if title is longer than one line

% \author{Davide Fissore}{Université Côte d'Azur, Inria, France}{davide.fissore@inria.fr}{https://orcid.org/0009-0002-5934-8776}{This work has been supported by the French government, through the France 2030 investment plan managed by the Agence Nationale de la Recherche, as part of the ``UCA DS4H'' project, reference ANR-17-EURE-0004.}

% \author{Enrico Tassi}{Université Côte d'Azur, Inria, France}{enrico.tassi@inria.fr}{https://orcid.org/0000-0002-7783-528X}{}

% \authorrunning{J. Open Access and J.\,R. Public} %TODO mandatory. First: Use abbreviated first/middle names. Second (only in severe cases): Use first author plus 'et al.'

% \Copyright{Jane Open Access and Joan R. Public} %TODO mandatory, please use full first names. LIPIcs license is "CC-BY";  http://creativecommons.org/licenses/by/3.0/

% \begin{CCSXML}
% <ccs2012>
%    <concept>
%        <concept_id>10003752.10010124.10010131.10010134</concept_id>
%        <concept_desc>Theory of computation~Operational semantics</concept_desc>
%        <concept_significance>500</concept_significance>
%        </concept>
%  </ccs2012>
% \end{CCSXML}

% \ccsdesc[500]{Theory of computation~Operational semantics}

% \keywords{Semantics, Prolog, Elpi, Determinacy Analysis, Rocq, Coq}

% \category{} %optional, e.g. invited paper

% \relatedversion{} %optional, e.g. full version hosted on arXiv, HAL, or other respository/website
% %\relatedversiondetails[linktext={opt. text shown instead of the URL}, cite=DBLP:books/mk/GrayR93]{Classification (e.g. Full Version, Extended Version, Previous Version}{URL to related version} %linktext and cite are optional

% %\supplement{}%optional, e.g. related research data, source code, ... hosted on a repository like zenodo, figshare, GitHub, ...
% %\supplementdetails[linktext={opt. text shown instead of the URL}, cite=DBLP:books/mk/GrayR93, subcategory={Description, Subcategory}, swhid={Software Heritage Identifier}]{General Classification (e.g. Software, Dataset, Model, ...)}{URL to related version} %linktext, cite, and subcategory are optional

% %\funding{(Optional) general funding statement \dots}%optional, to capture a funding statement, which applies to all authors. Please enter author specific funding statements as fifth argument of the \author macro.

% %\acknowledgements{I want to thank \dots}%optional

% %\nolinenumbers %uncomment to disable line numbering



% %Editor-only macros:: begin (do not touch as author)%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% \EventEditors{John Q. Open and Joan R. Access}
% \EventNoEds{2}
% \EventLongTitle{42nd Conference on Very Important Topics (CVIT 2016)}
% \EventShortTitle{CVIT 2016}
% \EventAcronym{CVIT}
% \EventYear{2016}
% \EventDate{December 24--27, 2016}
% \EventLocation{Little Whinging, United Kingdom}
% \EventLogo{}
% \SeriesVolume{42}
% \ArticleNo{23}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}

% \author{Davide Fissore}
% \email{davide.fissore@inria.fr}
% \orcid{0009-0002-5934-8776}
% \affiliation{
%   \institution{Université Côte d'Azur, Inria}
%   \country{France}
% }
%
% \author{Enrico Tassi}
% \email{enrico.tassi@inria.fr}
% \orcid{0000-0002-7783-528X}
% \affiliation{
%   \institution{Université Côte d'Azur, Inria}
%   \country{France}
% }
\author{Anonymous Author(s)}
\affiliation{
  \institution{Anonymous Institution}
  \country{Anonymous}
}
\email{anonymous@institute.com}

\title{Operational semantics for \prologP with Cut in \rocqP
and its application to determinacy analysis} % of \elpiP programs}

%TODO mandatory: add short abstract of the document
\begin{abstract}
We mechanize two operational semantics for \prologP with cut and prove their
equivalence in the \rocqP{} prover.

The first semantics is the one introduced by Debray and
Mishra~\cite{Debray1988} for studying the optimizations employed by
\prologP abstract machines~\cite{warren1983abstract,10.1007/BFb0105415}.
This semantics is based on a stack of alternatives, where the cut operator is
represented by a pointer to a shorter stack. 

The second semantics is novel and based on And-Or trees, in which the structure of
alternative computations is made explicit and preserved throughout execution.
This enables an explicit characterization of the cut operator as a function
that prunes selected branches of the tree. The tree-based semantics allows
us to formulate introduction and elimination rules for disjunction in the
presence of cut, which, as expected, differ from those of classical logic.

As a case study, we use the tree-based semantics to prove the soundness of
the determinacy analysis introduced in~\cite{fissore2026}. By exploiting the
equivalence between the two semantics, we relate this result to the
stack-based semantics used in efficient implementations of logic programming
such as~\cite{dunchev15lpar}.
\end{abstract}

\keywords{Semantics, Prolog, Elpi, Determinacy Analysis, Rocq, Coq}

\maketitle              % typeset the header of the contribution

\section{Introduction}

Backtracking is a compelling feature of logic programming, but it is also a
double-edged sword. On the one hand, it makes it possible to write concise
code that searches for a solution or enumerates all solutions. On the other
hand, backtracking is often a source of poor performance and can make logic
programs harder to debug. To mitigate these problems, the logic programming
community has studied various forms of determinacy
analysis~\cite{1989Warren,1996Somogy,1996henderson,2011king,lopez2004determinacy,hanus2025,fissore2026}
that identify logic programs that are deterministic, i.e., that compute
functions rather than relations: they commit to their first solution and
leave no \emph{choice points} (places where backtracking could resume). A
choice point corresponds to a suspended \emph{alternative} computation; in
the following, we use \emph{choice point} and \emph{alternative}
interchangeably.
The control operator \emph{cut} allows the programmer to discard
choice points and is therefore pervasively used to write deterministic code,
but it is also well known to make the semantics of the programming language
depart from a purely logical reading, thereby making reasoning about code
 harder.

Intuitively, nondeterminism arises when a computation can be completed using
two different rules. This situation is typically ruled out statically by
performing two complementary checks. The first one, called
\emph{mutual exclusion}, checks that at most one rule can apply to a given
query. This condition is satisfied if either (i) no two rules have
overlapping heads, so that no query can unify with both, or (ii) the
analysis accounts for the presence of cut in rule premises, so that once the
cut is executed all remaining rules are discarded. The second check, called
\emph{rule-body checking}, verifies that each rule either (i) calls only
functions, ensuring that no choice point remains after the premises of the
chosen rule have been executed, or (ii) executes a cut after calling
relations, in which case any choice points left by the call are discarded.

Most papers in the literature only provide paper proofs for the soundness
of their analysis, with the exception of Kriener and King~\cite{2011king}.
They mechanize in the \rocqP{} prover (formerly the \coqP{} proof assistant) a
denotational semantics for \prologP with cut and use it to prove the
soundness of an abstract-interpretation-based determinacy inference
algorithm. In addition to their \rocqP{} code being unavailable, this
mechanization has two further shortcomings. First, the analysis does not
scale to dynamic predicates (see \cite[Sec.~8]{fissore2026}), so it cannot be
applied to
\lprologP~\cite{1991miller-pf,Miller_Nadathur_2012} which is our long term goal. Second, there is
no mechanized link between their denotational semantics and the operational
semantics of Debray and Mishra~\cite{Debray1988}, which underlie standard
\prologP abstract machines~\cite{warren1983abstract,1991ait-wam,10.1007/BFb0105415}.

\paragraph{Contributions}
In this paper we mechanize in \rocqP{} two operational semantics for
\prologP with cut. The first is the stack-based semantics given by Debray
and Mishra~\cite{Debray1988}. This semantics is closely related to actual \prologP
implementations, but it makes reasoning about the scope of cut difficult:
alternatives are stored in a stack, in chronological order, and the cut
operator is represented as a pointer to a shorter stack. The relation
between the rules being cut and the affected stack frames is therefore not
immediately visible.

To address this limitation, we introduce a semantics based on And-Or trees,
in which the history of the computation is represented in a structured way:
given a goal, each applicable rule generates an Or-branch, which is in turn
followed by an And-branch for each premise. This enables an explicit
characterization of the cut operator as a function that prunes selected
branches of the tree. The tree-based semantics allows us to formulate
introduction and elimination rules for disjunction in the presence of cut,
which, as expected, differ from those of classical logic. For example,
$q_1 \lor q_2$ is not equivalent to $q_2 \lor q_1$, since $q_2$ may be cut
away by $q_1$.

Finally, we use the tree-based semantics to prove the soundness of the
determinacy analysis from~\cite{fissore2026}. In \rocqP{}, we prove that any
terminating execution of a predicate identified as deterministic introduces no
additional choice points. Combined with the equivalence
between the two semantics, this result implies that any call to a
deterministic predicate leaves no choice points on the stack used by
actual \prologP implementations, including the
\elpiP~\cite{dunchev15lpar} \lprologP{} interpreter, for which the
static analysis of~\cite{fissore2026} was originally developed. \elpiP
serves as an extension language for the \rocqP{} prover and has become an
important component of the \rocqP{} ecosystem~\cite{krebbers25,Trakt,Maas2024,fissore2023,fissore2024,cohen_et_al:LIPIcs.FSCD.2020.34,Trocq,TrocqJ,tassi2018,tassi2019,tassi2023};
our result therefore further strengthens confidence in its
implementation.

Our mechanization is axiom-free and is available at\\
% \url{https://github.com/FissoreD/elpi-formalization}.
\url{ANONYMIZED} (included as supplementary material).

\begin{comment}
\section{Introduction}


\elpiP is a dialect of \lprologP (see \cite{1991miller-pf,Miller_Nadathur_2012,dunchev15lpar,TASSI_2019})
used as an extension language for the \rocqP prover (formerly the \coqP proof
assistant). \elpiP has become an important infrastructure component: several
projects and libraries depend on it~\cite{krebbers25,Trakt,Maas2024,fissore2023,fissore2024}.
Other notable Rocq libraries based on \elpiP
include the Hierarchy-Builder library-structuring tool~\cite{cohen_et_al:LIPIcs.FSCD.2020.34};
the Trocq proof-transfer tool~\cite{Trocq,TrocqJ}
and Derive~\cite{tassi2018,tassi2019,tassi2023}, a program-and-proof synthesis
framework with industrial applications at SkyLabs AI.

Starting with version 3, \elpiP is equipped with a static analysis for
determinacy~\cite{fissore2026} to help users control backtracking.
\rocqP{} users are familiar with functional programming, but not necessarily
with logic programming; in particular, uncontrolled backtracking is a common
source of inefficiency and makes debugging harder. The determinacy checker
allows users to specify which predicates should be considered deterministic
and then verifies that these predicates behave like functions, i.e., that
they commit to their first solution and leave no \emph{choice points}
(places where backtracking could resume). A choice point corresponds to a
suspended, \emph{alternative}, computation. In the following, we
use \emph{choice point} and \emph{alternative} as synonyms.

Intuitively, nondeterminism arises when a computation can be completed using
two different rules. The static analysis in~\cite{fissore2026} excludes this
situation in two steps. The first step, called \emph{mutual exclusion},
checks that at most one rule can apply to a given query. This condition is
satisfied if either (i) no two rules have overlapping heads, so that no
query can unify with both, or (ii) the analysis accounts for the presence of
cut in rule premises, so that once the cut is executed all remaining rules
are discarded. The second step, called \emph{rule checking}, verifies that
each rule either (i) calls only functions, ensuring that no choice point
remains after the premises of the chosen rule have been executed, or (ii)
executes a cut after calling relations, in which case any choice points left
by the call are discarded.

The static analysis introduced in~\cite{fissore2026} is supported by
benchmarks that demonstrate its usefulness, but it is not accompanied by a
proof of soundness. This paper reports our first steps toward mechanizing
the soundness of this analysis. In particular, we focus on the control
operator \emph{cut}, which is pervasively used to write deterministic code
and causes the semantics to depart from a purely logical reading.

We mechanize two operational semantics for \prologP with cut. The first is a
stack-based semantics that closely models \elpiP's implementation and is
essentially the semantics given by Debray and
Mishra~\cite[Sec.~4.3]{Debray1988}, which underlies standard \prologP
abstract machines~\cite{warren1983abstract,1991ait-wam,10.1007/BFb0105415}.
This semantics admits efficient implementations, but it makes reasoning
about the scope of cut difficult: alternatives are stored in a stack, in
chronological order, and the cut operator is represented as a pointer to a
shorter stack. The relation between the rule being cut and the affected
stack frames is therefore not immediately visible.

To address this limitation, we introduce a semantics based on And-Or trees,
in which the history of the computation is represented in a structured way:
given a goal, each applicable rule generates an Or-branch, which is in turn
followed by an And-branch for each premise.
This enables an explicit characterization of the cut operator as a function
that prunes selected branches of the tree. The tree-based semantics allows
us to formulate introduction and elimination rules for disjunction in the
presence of cut, which, as expected, differ from those of classical logic.
For example, $q_1 \lor q_2$ is not equivalent to $q_2 \lor q_1$,
since $q_2$ may be cut away by $q_1$.

Finally, we use the tree-based semantics to prove the correctness of the
static determinacy analysis from~\cite{fissore2026}. We prove in Rocq that
any call to a predicate identified as deterministic leaves no additional
Or-branches in the tree when it terminates. By composing this result with
the equivalence between the two semantics, we obtain that any call to a
deterministic predicate leaves no choice points on the stack used by the
\elpiP interpreter.

Our mechanization is axiom-free and is available at 
% \url{https://github.com/FissoreD/elpi-formalization}.
\url{ANONYMEZED_FOR_BLINDNESS}
\end{comment}
% questa deve stare vicino all'intro perchè aiuta a capire la det

\section{Preliminaries: syntax and informal semantics of cut}
\label{sec:preliminaries}
% \begin{figure}[b]
% \begin{coqcode}
% Inductive Tm := Symb of S | Pred of P | Var of V | App of Tm & Tm.
% Inductive Atom := cut | call of Tm.
% Record R := mkR { head : Tm; premises : list Atom }.
% Record ~\ensuremath{\mathbb{P}}~ := { rules : seq R; sig : sigT }.
% Definition ~\ensuremath{\Sigma}~ := {fmap V -> Tm}.
% \end{coqcode}
% \vspace{-1.4em}
%   % \caption{Syntax of \elpiP programs}
%   % \end{minipage}
%   % \begin{minipage}{.45\textwidth}    
%   % \input{tex_code/atom_type.tex}\\
%   % \input{tex_code/R_type.tex}\\
%   % \input{tex_code/program_type.tex}\\
%   % \input{tex_code/sigma_type.tex}
% \caption{Definition of term, rule, program and substitution}
%   \label{type:atom}
%   \label{type:r}
%   \label{type:prog}
%   \label{type:sigma}
%   \label{fig:basex}
% % \end{minipage}

% \end{figure}

In the entire paper we use \cref{fig:progx} as our running example, and
we start by describing how we represent a program like that one.
In the concrete syntax, variables are written with capital letters, and
predicate application follows the $\lambda$-calculus style (no
parentheses).

The description of a program $\PROG$ is shared by both semantics
and consists of a list of
rules together with a signature environment.
We delay the discussion of signatures to \cref{sec:determinacy} where we
describe the static analysis that concerns them and we now focus
on the rules.
% is given by a pair $\PROG := (\mathrm{list}\ \mathbb{R} \times \mathrm{sigT})$
% and is shared by both semantics. A program consists of a list of
% rules together with a signature environment $\mathrm{sig}$.
% We delay the discussion of signatures to \cref{sec:determinacy} where we
% describe the static analysis that concerns them.

A rule $\RULE$ is a pair $\TERM \times \mathrm{list}\ \ATOM$, where
the first element, a term, represents the rule's head and the second,
a list of atoms, represents the rule's premises.
%
%
\begin{align*}
  \TERM & := \mathrm{Symb}\ \mathbb{N} \mid \mathrm{Pred}\ \mathbb{N} \mid \mathrm{Var}\ \mathbb{N} \mid \mathrm{App}\ \TERM\ \TERM\\
  \ATOM & := \mathrm{cut} \mid \mathrm{call}\ \TERM
\end{align*}

An atom is either the cut operator or a call to a term.
Terms include predicate symbols, data symbols, variables, and term application.

The syntax tree allows variables to be applied and predicates to be mixed with data.
These higher-order features play no role in the current paper, which focuses
on first-order logic programming, but they allow future extensions to full
\lprologP{} to be based on the same mechanization.


Variables, predicates and data symbols
are identified by natural numbers. We represent substitutions
(of type $\Sigma$) as finitely supported maps from variables to terms, using the
finmap library~\cite{finmap}.
The empty substitution is written $\epsilon$,
while the composition of two substitutions $\sigma_1$ and $\sigma_2$ with
disjoint supports is written $\sigma_1 + \sigma_2$. %\marginpar{usiamo + ?}
 
The backchaining operation,
central to both semantics,
applies all rules to a query term:
%\input{tex_code/bc_type.tex}
%
$$\mathrm{backchain} : \mathbb{P} \to \FV \to \TERM \to \Sigma \to (\FV \times \mathrm{list}\ (\Sigma \times \mathrm{list}\ \ATOM)) $$

Since a rule may be applied multiple times, its variables must be
freshened before each application. Accordingly, the backchain function
takes a program, the set of variable names used so far (\FV), a
query term, and the current substitution. It returns an updated
set of variable names together with the list of alternatives, i.e. the
rules that apply. More precisely, for each applicable rule it returns the
rule premises together with an updated substitution obtained by unifying
the rule head with the query term.
The unification function takes two terms $t_1$ and $t_2$, together with a
substitution $\sigma$, as input, and optionally returns a substitution
$\sigma'$ that extends $\sigma$. If $t_1$ and $t_2$ unify
starting from $\sigma$, then $\sigma'~t_1 = \sigma'~t_2$, where
$\sigma~t$ denotes substitution application and $=$ denotes 
syntactic equality.

To keep the mechanization axiom-free, we implement a unification
algorithm, which we briefly describe in~\cref{sec:determinacy} and prove
sound and complete.
Since the two semantics and the static analysis use the same
unification algorithm, the equivalence proof is completely agnostic to
the particular implementation chosen for that algorithm.

% \input{tex_code/unif_type.tex}
% TODO: state axioms and explain matching; cite Dale's unification results.
% In this paper we assume the reader is familiar with first-order
% unification.

% A backchain is performed when the current
% atom is a \texttt{call}. In this case, it start by recursively
% \emph{dereferencing} the variables in`' $\sigma$, i.e. replacing all the variables
% in the query by their term mapping in $\sigma$.


% \begin{figure}
%   \input{tex_code/base_type.tex}

%   \begin{minipage}{0.49\textwidth}
%     \input{tex_code/tm_type.tex}
%     \caption{Tm types}
%     \label{fig:tm-type}
%   \end{minipage}
%   \begin{minipage}{0.45\textwidth}
%     \input{tex_code/unif_type.tex}
%     \caption{Unif type}
%     \label{fig:unif-type}
%   \end{minipage}
% \end{figure}


% Before going to the two semantcis, we show the piece of data structure that
% are shared by the them. The smallest unit of code that we can use in the
% langauge is an atom. The atom inductive (see~\cref{type:atom}) is
% either a cut or a call. A call
% carries a term~(see \cref{fig:tm-type}). A term (Tm) is either a predicate,
% a datum, a variable or the binary application of two terms.

% \begin{definition}[\logo{}Atoms]
%   \input{tex_code/atom_type.tex}
%   \label{type:atom}
% \end{definition}
% \begin{definition}[\logo{}Rule]
%   \input{tex_code/R_type.tex}
%   \label{type:r}
% \end{definition}
% \begin{definition}[\logo{}Program]
%   \input{tex_code/program_type.tex}
%   \label{type:prog}
% \end{definition}
% \begin{definition}[\logo{}Substitution]
%   \input{tex_code/sigma_type.tex}
%   \label{type:sigma}
% \end{definition}
% \begin{definition}[\logo{}Backchaining]
%   \leavevmode\\
%   \phantom{AAAA}\input{tex_code/bc_type.tex}
%   \label{type:backchain}
% \end{definition}

% \mvspace{}
% \deftypes[type:atom]{tex_code/atom_type.tex}
% \deftypes[type:r]{tex_code/R_type.tex}
% \deftypes[type:prog]{tex_code/program_type.tex}
% \deftypes[type:sigma]{tex_code/sigma_type.tex}
% \deftypes[type:backchain]{tex_code/bc_type.tex}
% \mvspace{}

% A rule (see~\cref{type:r}) has a head of type term and a list of premises, that\marginpar{se riesco extract lemma immediately with lemma environment}
% are a list of atoms. A program (see~\cref{type:prog}) is made by its rules and
% the signatures of predicates, \texttt{sigT} is a mapping from predicates to
% their signatures, signatures represent the defintion of type from the simply
% typed lambda calculus, i.e. it is either a base type or an arrow for term
% application. We decorate arrows to know the mode (input or output) of the
% application argument.

% A substitution (see \cref{type:sigma}) is a mapping from variables to terms.
% It is the output of a successful query. The empty substitution is noted
% $\epsilon$.

% The backchain function (backchain, see~\cref{type:backchain}) takes: a
% program $P$; a set $fv$ of variables ($\mathcal{F}_{\!\!\nu}$); a query
% $q$; and the substitution $\sigma$. A backchain is performed when the current
% atom is a \texttt{call}. In this case, it start by recursively
% \emph{dereferencing} the variables in $\sigma$, i.e. replacing all the variables
% in the query by their term mapping in $\sigma$.

% TODO: free variables and program freshness

% Then it takes the head of the
% term, which should be rigid, i.e. a predicate $p$, and look for the signature
% $s$ of $p$ in the program. Then, for each rule $r$ in $P$, it unifies the head
% of $r$ with $q$. Unification is possible thanks to the unifcator $U$, whose type
% is shown in~\cref{fig:unif-type}. $U$ explains how to unify terms up to standard
% unifcation (for output terms) or matching (for input terms). The function
% \emph{unif} (resp. \emph{matching}) unifies (resp. matches) the two terms under
% $\sigma$, it return an extension of $\sigma$ if the two terms can be unified
% (resp. matched), and None otherwise. The result of a backchain is made by
% filtering the rules in $P$ that can be used on the given query. In particular,
% for each rule filtered rules $r$, it returnes a pair containing the extension of
% $\sigma$ making the head of $r$ and $q$ unify and the list of premises of $r$.

\begin{figure*}[t]
  \begin{minipage}{0.48\textwidth}
    \inputminted[fontsize=\footnotesize,autogobble,escapeinside=~~,mathescape=true,frame=leftline,framerule=0pt,framesep=1em]{elpi.py:ElpiLexer}{./progx.elpi}
  \end{minipage}
  \begin{minipage}{0.48\textwidth}
    \inputminted[fontsize=\footnotesize,autogobble,escapeinside=~~,mathescape=true,frame=leftline,framerule=0pt,framesep=1em]{elpi.py:ElpiLexer}{./progy.elpi}
  \end{minipage}\vfill
    \caption{Running example of a logic program in \elpiP syntax}
    \label{fig:progx}
  \end{figure*}

\subsection{The cut operator}
\label{sec:cut-op}

There are several variants of the cut operator in the literature. The
most widely adopted one is the \emph{hard cut}
(see, e.g., the documentation of \sprologP~\cite{swi-prolog-cut}).
Whenever the backchain function returns a non-empty list of alternatives,
the first is explored immediately, while the others are suspended as
choice points. Within a given alternative, premises are processed from
left to right, executing each atom in turn. When a cut is encountered,
it discards (i) all choice points created by backchaining for the current
call and (ii) all choice points created while executing the premises to
the left of the cut.

For example, consider the program in \cref{fig:progx} and the query
$\elpiIn{g 0 R}$.
Both rules for the predicate \eI{g} apply to this query. Backchaining therefore returns the
following two alternatives:
\[
  [(\sigma_1, [\eI{r A B, f B C, !}]),\; (\sigma_2, [\eI{f D E, f E F}])]
\]
with
$
  \sigma_1 \coloneq \{\eI{A} \mapsto \eI{0}, \eI{C} \mapsto \eI{R}\}
$
and
$
  \sigma_2 \coloneq \{\eI{D} \mapsto \eI{0}, \eI{F} \mapsto \eI{R}\}
$.
For simplicity, we choose an example where variable refreshing plays no role,
so we do not discuss the set \FV{} any further.

Execution continues with the first premise of the first alternative, namely
\eI{r A B} under $\sigma_1$, i.e. \eI{r 0 B}. The remaining alternatives are
stored as choice points.
Backchaining \eI{r 0 B} returns
$
  [(\sigma_3, []), (\sigma_4, []), (\sigma_5, [\eI{!}])]
$
where
$
  \sigma_3 \coloneq \sigma_1 + \{\eI{B} \mapsto \eI{0}\}
$,
$
  \sigma_4 \coloneq \sigma_1 + \{\eI{B} \mapsto \eI{1}\}
$
and
$
  \sigma_5 \coloneq \sigma_1 + \{\eI{B} \mapsto \eI{2}\}
$.

The next step of interpretation continues with \eI{f B C} under $\sigma_3$,
that is, \eI{f 0 R}, and leaves $[(\sigma_4, []), (\sigma_5, [\eI{!}])]$ as new choice points.
This time, backchaining fails (no rule for \eI{f} matches input $0$). We
therefore backtrack to the most recently introduced choice point and retry
\eI{f B C} under $\sigma_4$, i.e. \eI{f 1 R}. This succeeds with
$\sigma_6 \coloneq \sigma_4 + \{\eI{R} \mapsto \eI{2}\}$.

We now reach the cut. At this point there are still two unexplored
alternatives: one from the third rule for \eI{r} and one from the second rule
for \eI{g} (respectively, $\sigma_5$ and $\sigma_2$). Both are discarded,
because they were created when, or after, the first rule for \eI{g} was
selected. In contrast, a cut does not discard choice points created earlier;
in this example, there are none.
The final solution is:
$
  \{\eI{A} \mapsto \eI{0}, \eI{B} \mapsto \eI{1}, \eI{C} \mapsto \eI{R},
  \eI{R} \mapsto \eI{2}\}
$.

In \cref{tree-sem,stack-sem}, we provide fully mechanized operational semantics for
\prologP with cut. They differ in how they represent the ongoing computation,
in particular how alternatives are stored and how they are pruned by cut.

% We propose two formal, operational semantics for a logic program with cut. 
% The two
% semantics are based on different syntaxes, the first syntax (called tree)
% exploits a tree-like structure and is ideal both to have a graphical view of its
% evolution while the tree is being interpreted and to prove lemmas over it. The
% second syntax, called elpi, is the \elpiP's syntax and has the advantage of
% reducing the computational cost of cutting and backtracking alternatives by
% using shared pointers. We aim to prove the equivalence of the two semantics
% together with some interesting lemmas of the cut behavior.

\vspace{10pt}
\textit{Notational conventions}
In the following section we note $\mathbb{B}$ for the boolean type. 
Following the Mathematical Components library~\cite{assia_mahboubi_2022_7118596}, on which we depend,
we use \cI{x.1} and \cI{x.2} to denote the first and second projection
applied to the pair \cI{x}.
We use the notation $x \cap y =\varnothing$ 
to denote that the two sets $x$ and $y$ are disjoint. When the
arguments are terms, as in $t_1 \cap t_2 = \varnothing$, we mean that the two
terms do not share any variables. Similarly $t \subseteq v$ means that
the set of variables occurring in $t$ is included in the set $v$;
$\sigma \subseteq v$ means that $v$ includes the domain of $\sigma$ and the
all variables occurring in its codomain.



% List concatenation is the infix \mappend,
% while mapping a function \cI{f} on a list \cI{l} is written
% \cI{[seq f x | x <- l]}.
% \marginpar{spiegare notation for list}
% $\epsilon$ is the empty substitution.

\section{Stack-based semantics}
\label{stack-sem}

The stack-based semantics we present is essentially the one 
by Debray and Mishra in
\cite[Section~4.3]{Debray1988}. The main difference is that we hide the search for
applicable rules in the backchain auxiliary function.

%  and our mechanization closely follows
% the one of Pusch in \isabelleP{}
% \cite{10.1007/BFb0105415}. Pusch mechanized this
% semantics together with several optimizations that are
% also present in the Warren Abstract Machine
% \cite{warren1983abstract,1991ait-wam}.

The execution state is a stack of alternatives.
%
$$\mathrm{alts} := \mathrm{list}\ (\Sigma \times \mathrm{goals})  \qquad \mathrm{goals} := \mathrm{list}\ (\ATOM \times \mathrm{alts})$$

Intuitively each stack item is a self-contained
computation, coupling a substitution with all the goals to be
solved.
%
The datatypes of goals and alternatives are mutually recursive since
a goal pairs an atom with a list of the \emph{cut-to} alternatives. 
Still, these alternatives have a very different role: they have to be understood
as pointers to lower levels of the stack itself.
They are used to implement the cut, i.e. to prune the stack of alternatives.
The \nur inductive
predicate describes
the stack-based semantics.
%
\newcommand{\backchainS}{
  \ensuremath{\mathcal{B}_S}
}
%
% We are completely loosing the tree structure. There are no clean reset
% points. The backtracking operation is simpler: it is the tail function.
% The cutr and cutl operations disappears: the alternatives are stored 
% directly in the cutE terminal.
%\noindent
%
% %
% % \mvspace{}
% % \deftypes[type:nur]{tex_code/nur_type.tex}
% % \mvspace{}
% % \input{tex_code/elpi_def.tex}
% \begin{center}
% \begin{coqcode}
% ~\ensuremath{\qquad\qquad}~ Inductive alts  := nilA | consA of (~\ensuremath{\Sigma}~ * goals)  & alts
% ~\ensuremath{\qquad\qquad}~ with      goals := nilG | consG of (Atom * alts) & goals 
% \end{coqcode}
% \end{center}
%
$$\nur : \mathbb{P} \to \FV \to \mathrm{alts} \to \mathrm{option}\ (\Sigma \times \mathrm{alts}) \to \cI{Prop}$$

\noindent
The \nur predicate relates a program, a set of variables and a
list of alternatives to an optional result. None stands for failure,
i.e., no solution, whereas Some $(\sigma, a)$ represents success with $\sigma$
being the solution and $a$ as the list of not-yet-explored
alternatives.
\begin{figure}[b]
   \begin{align*}
  \backchainS\ p\ v\ t\ & \sigma\ a\ g =\ 
  \mathrm{let}\ (v', rs) := \mathrm{backchain}\ p\ v\ t\ \sigma\ \mathrm{in}\\
  &\bigg(v', \bigg[
  \sigma',
  (
    [(q, a) \mid q \in b] \mappend g
  )
  \ \bigg|\ 
  (\sigma', b) \in rs
  \bigg]\bigg)
  \end{align*}\vspace{.5em}

  % \input{tex_code/nurbp.tex}

\begin{prooftree}
	\AxiomC{$~$}
  \RightLabel{$\stops $}
  \UnaryInfC{$\nur\ p\ v\ ( ( \sigma ,\ \mnil )\ \mathrm{::}\ a )\ ( \mathrm{Some}\ ( \sigma ,\ a ) ) $}
\end{prooftree}
\begin{prooftree}
	\AxiomC{$\nur\ p\ v\ ( ( \sigma ,\ g )\ \mathrm{::}\ \mathrm{ca} )\ r $}
  \RightLabel{$\cuts $}
  \UnaryInfC{$\nur\ p\ v\ ( ( \sigma ,\ ( \mathrm{cut} ,\ \mathrm{ca} )\ \mathrm{::}\ g )\ \mathrm{::}\ \_ )\ r $}
\end{prooftree}
\begin{prooftree}
	\AxiomC{$\backchainS\ p\ v\ t\ \sigma\ a\ g = ( v' ,\ \mathrm{bs} )  \qquad \nur\ p\ v'\ ( \mathrm{bs}\ \mappend\ a )\ r $}
  \RightLabel{$\calls $}
  \UnaryInfC{$\nur\ p\ v\ ( ( \sigma ,\ ( \mathrm{call}\ t ,\ \_ )\ \mathrm{::}\ g )\ \mathrm{::}\ a )\ r $}
\end{prooftree}
\begin{prooftree}
	\AxiomC{$~$}
  \RightLabel{$\fails $}
  \UnaryInfC{$\nur\ \_\ \_\ \mnil\ \mathrm{None} $}
\end{prooftree}
  \caption{Rule system for \nur}
  \label{fig:nur-rules}
\end{figure}


The rule system for \nur is given in \cref{fig:nur-rules} and consists of four
cases. We distinguish two main cases. If we run out of alternatives
we fail: rule \fails stops the execution and returns None.
If the list of alternatives is $(\sigma_0, h) :: a$, then
we look at the list of goals $h$ (under the substitution $\sigma_0$). If $h$ is empty, rule \stops stops the execution with a success $\sigma_0$
leaving $a$ as the unexplored alternatives.
If $h$ is not empty we look at its head goal $(t, ca)$ where $t$ is the atom and $ca$ is its
cut-to alternatives. If $t$ is a cut, rule
\cuts continues to evaluate the tail of $h$ with alternatives $ca$. Finally, when $t$ is a call, rule \calls backchains:
for each rule it pairs each premise with the current stack of
alternatives, and prepends the obtained goals to the tail of $h$.
Note that if backchain returns the empty list, the second
premise of rule \calls amounts to exploring the next item in the current
stack.

\newsavebox{\elpibox}
\begin{figure*}[!htb]
  \centering
  \begin{subfigure}{\textwidth}
    \centering
    \begin{lrbox}{\elpibox}
    \begin{tikzpicture}[baseline]
    \matrix (outer0) at (0,0) [matrix of nodes,
                     nodes={draw, minimum width=0.5cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, anchor=center, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.3cm, row sep=0.3cm] {
    g 0 R\\
    };
    \node[font=\bfseries] at ($(outer0.west)+(-0.4,0)$) {3a)};

    \matrix (outer1) at (7,0) [matrix of nodes,
                     nodes={draw, minimum width=0.5cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, anchor=center, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.3cm, row sep=0.3cm] {
    \phantom{AAAAAAAAAAAAAA} & \phantom{AAAAAAAAAAA} &  \node[draw=none] (A1) {\phantom{!}};\\
    };
    \node[font=\bfseries] at ($(outer1.west)+(-0.4,0)$) {3b)};
    \matrix (inner1a) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer1-1-1.center) {
    r A B, & f B C, & ! \\
    };
    \matrix (inner1b) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer1-1-2.center) {
    f D E, & f E F \\
    };
    \matrix (inner1c) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (A1.center) {
    \mphant \\
    };
    \draw[->, thick]
      ($(inner1a-1-3.north)+(0,0.3)$) -- ++(0,0.4)
      -- ($(inner1c-1-1.north)+(0,0.7)$)
      -- ($(inner1c-1-1.north)+(0,0.3)$);
    \node at ($(outer1-1-1.north west)+(-0,0.2)$) {$\sigma_1$};
    \node at ($(outer1-1-2.north west)+(-0,0.2)$) {$\sigma_2$};

    \matrix (outer2) at (0,-2.5) [matrix of nodes,
                     nodes={draw, minimum width=0.5cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, anchor=center, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.3cm, row sep=0.3cm] {
    \phantom{AAAAAAAA} & \phantom{AAAAAAAA} & \phantom{AAAAAAAAAAA} & \phantom{AAAAAAAAA} &  \node[draw=none] (A2) {\phantom{xx}};\\
    };
    \node[font=\bfseries] at ($(outer2.west)+(-0.4,0)$) {3c)};
    \matrix (inner2a) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer2-1-1.center) {
    f B C, & ! \\
    };
    \matrix (inner2b) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer2-1-2.center) {
    f B C, & ! \\
    };
    \matrix (inner2c) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer2-1-3.center) {
    !, & f B C, & ! \\
    };
    \matrix (inner2d) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer2-1-4.center) {
    f D E, & f E F \\
    };
    \matrix (inner2L) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (A2.center) {
    \mphant \\
    };
    \draw[->, thick]
      ($(inner2a-1-2.north)+(0,0.3)$) -- ++(0,0.6)
      -- ($(inner2L-1-1.north)+(0,0.9)$)
      -- ($(inner2L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner2b-1-2.north)+(0,0.3)$) -- ++(0,0.5)
      -- ($(inner2L-1-1.north)+(0,0.8)$)
      -- ($(inner2L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner2c-1-3.north)+(0,0.3)$) -- ++(0,0.4)
      -- ($(inner2L-1-1.north)+(0,0.7)$)
      -- ($(inner2L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner2c-1-1.south)-(0,0.3)$) -- ++(0,-0.4)
      -- ($(inner2d-1-1.south)-(0,0.7)$)
      -- ($(inner2d-1-1.south)-(0,0.3)$);
    \node at ($(outer2-1-1.north west)+(-0,0.2)$) {$\sigma_3$};
    \node at ($(outer2-1-2.north west)+(-0,0.2)$) {$\sigma_4$};
    \node at ($(outer2-1-3.north west)+(-0,0.2)$) {$\sigma_5$};
    \node at ($(outer2-1-4.north west)+(-0,0.2)$) {$\sigma_2$};

    \matrix (outer3) at (10,-2.5) [matrix of nodes,
                     nodes={draw, minimum width=0.5cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, anchor=center, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.3cm, row sep=0.3cm] {
    \phantom{AAAAAAAA} & \phantom{AAAAAAAAAA} & \phantom{AAAAAAAAAA} &  \node[draw=none] (A3) {\phantom{xx}};\\
    };
    \node[font=\bfseries] at ($(outer3.west)+(-0.4,0)$) {3d)};
    \matrix (inner3a) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer3-1-1.center) {
    f B C, & ! \\
    };
    \matrix (inner3b) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer3-1-2.center) {
    !, & f B C, & ! \\
    };
    \matrix (inner3c) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer3-1-3.center) {
    f D E, & f E F \\
    };
    \matrix (inner3L) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (A3.center) {
    \mphant \\
    };
    \draw[->, thick]
      ($(inner3a-1-2.north)+(0,0.3)$) -- ++(0,0.5)
      -- ($(inner3L-1-1.north)+(0,0.8)$)
      -- ($(inner3L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner3b-1-3.north)+(0,0.3)$) -- ++(0,0.4)
      -- ($(inner3L-1-1.north)+(0,0.7)$)
      -- ($(inner3L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner3b-1-1.south)-(0,0.3)$) -- ++(0,-0.4)
      -- ($(inner3c-1-1.south)-(0,0.7)$)
      -- ($(inner3c-1-1.south)-(0,0.3)$);
    \node at ($(outer3-1-1.north west)+(-0,0.2)$) {$\sigma_4$};
    \node at ($(outer3-1-2.north west)+(-0,0.2)$) {$\sigma_5$};
    \node at ($(outer3-1-3.north west)+(-0,0.2)$) {$\sigma_2$};

    \matrix (outer4) at (0,-5.5) [matrix of nodes,
                     nodes={draw, minimum width=0.5cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, anchor=center, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.3cm, row sep=0.3cm] {
    \phantom{AAAAAAAA} & \phantom{AAAAAAAAAA} & \phantom{AAAAAAAAAA} &  \node[draw=none] (A4) {\phantom{xx}};\\
    };
    \node[font=\bfseries] at ($(outer4.west)+(-0.4,0)$) {3e)};
    \matrix (inner4a) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer4-1-1.center) {
    f B C, & ! \\
    };
    \matrix (inner4b) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer4-1-2.center) {
    !, & f B C, & ! \\
    };
    \matrix (inner4c) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer4-1-3.center) {
    f D E, & f E F \\
    };
    \matrix (inner4L) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (A4.center) {
    \mphant \\
    };
    \draw[->, thick]
      ($(inner4a-1-2.north)+(0,0.3)$) -- ++(0,0.5)
      -- ($(inner4L-1-1.north)+(0,0.8)$)
      -- ($(inner4L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner4b-1-3.north)+(0,0.3)$) -- ++(0,0.4)
      -- ($(inner4L-1-1.north)+(0,0.7)$)
      -- ($(inner4L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner4b-1-1.south)-(0,0.3)$) -- ++(0,-0.4)
      -- ($(inner4c-1-1.south)-(0,0.7)$)
      -- ($(inner4c-1-1.south)-(0,0.3)$);
    \node at ($(outer4-1-1.north west)+(-0,0.2)$) {$\sigma_6$};
    \node at ($(outer4-1-2.north west)+(-0,0.2)$) {$\sigma_5$};
    \node at ($(outer4-1-3.north west)+(-0,0.2)$) {$\sigma_2$};

    \matrix (outer5) at (9,-5.5) [matrix of nodes,
                     nodes={draw, minimum width=0.5cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, anchor=center, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.3cm, row sep=0.3cm] {
    \phantom{AAA} & \phantom{AAAAAAAAAA} & \phantom{AAAAAAAAAA} &  \node[draw=none] (A5) {\phantom{xx}};\\
    };
    \node[font=\bfseries] at ($(outer5.west)+(-0.4,0)$) {3f)};
    \matrix (inner5a) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer5-1-1.center) {
    ! \\
    };
    \matrix (inner5b) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer5-1-2.center) {
    !, & f B C, & ! \\
    };
    \matrix (inner5c) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (outer5-1-3.center) {
    f D E, & f E F \\
    };
    \matrix (inner5L) [matrix of nodes,
                     nodes={minimum width=0.6cm, minimum height=0.7cm, text height=1.5ex, text depth=.25ex, font=\fontsize{8pt}{9.6pt}\selectfont},
                     column sep=0.1cm, row sep=0.1cm] at (A5.center) {
    \mphant \\
    };
    \draw[->, thick]
      ($(inner5a-1-1.north)+(0,0.3)$) -- ++(0,0.5)
      -- ($(inner5L-1-1.north)+(0,0.8)$)
      -- ($(inner5L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner5b-1-3.north)+(0,0.3)$) -- ++(0,0.4)
      -- ($(inner5L-1-1.north)+(0,0.7)$)
      -- ($(inner5L-1-1.north)+(0,0.3)$);
    \draw[->, thick]
      ($(inner5b-1-1.south)-(0,0.3)$) -- ++(0,-0.4)
      -- ($(inner5c-1-1.south)-(0,0.7)$)
      -- ($(inner5c-1-1.south)-(0,0.3)$);
    \node at ($(outer5-1-1.north west)+(-0,0.2)$) {$\sigma_6$};
    \node at ($(outer5-1-2.north west)+(-0,0.2)$) {$\sigma_5$};
    \node at ($(outer5-1-3.north west)+(-0,0.2)$) {$\sigma_2$};
    \end{tikzpicture}
    \end{lrbox}
    \resizebox{\textwidth}{!}{\usebox{\elpibox}}
  \end{subfigure}

  \begin{subfigure}{0pt}\phantomcaption\label{ex-elpi0}\end{subfigure}%
  \begin{subfigure}{0pt}\phantomcaption\label{ex-elpi1}\end{subfigure}%
  \begin{subfigure}{0pt}\phantomcaption\label{ex-elpi2}\end{subfigure}%
  \begin{subfigure}{0pt}\phantomcaption\label{ex-elpi3}\end{subfigure}%
  \begin{subfigure}{0pt}\phantomcaption\label{ex-elpi4}\end{subfigure}%
  \begin{subfigure}{0pt}\phantomcaption\label{ex-elpi5}\end{subfigure}%
  \caption{Execution in the stack semantics. 
  The substitutions $\sigma_1\dots\sigma_6$ are from \cref{sec:cut-op}}.
  \label{ex-elpi}
\end{figure*}
\subsection{Example of execution}

We propose an example of the execution of \nur in \cref{ex-elpi}. The
arrows in the images represent the cut-to pointers.

We have not drawn arrows coming out of call atoms since the interpreter
ignores the cut-to alternatives in this case.
The evolution
of the stack in \cref{ex-elpi} mirrors the example in \cref{sec:cut-op}:
we evaluate the first goal of the first alternative. 
During backchaining,
we add a pointer to the remaining alternatives to each new cut operator.

If a cut is evaluated, we replace the remaining alternatives with the
cut-to alternatives. For example, in \cref{ex-elpi5}, evaluating the cut
discards the second and third elements in the stack. Backtracking is
performed by simply consuming the first alternative from the stack, as shown
in \cref{ex-elpi3}.


\section{And-Or Tree semantics}
\label{tree-sem}
% \begin{figure}[b]
%     % \input{tex_code/tree_def.tex}
% %     \begin{coqcode}
% % Inductive tree ~$\coloneq$~
% %   | Bot | Top                            (* failure and success *)
% %   | Todo of Atom                       (* unexplored branch *)
% %   | Or   of option tree & ~$\Sigma$~ & tree ~$\!$~    (* $A \lor B_\sigma$ *) 
% %   | And  of tree & list Atom & tree.   (* $A \land_{B_0} B$ *)
% % \end{coqcode}
%   $$\TREE := \bot \mid \top \mid \mathrm{Todo}\ \ATOM \mid \mathrm{Or}\ (\mathrm{option}\ \TREE)\ \Sigma\ \TREE \mid \mathrm{And}\ \TREE\ (\mathrm{list}\ \ATOM)\ \TREE$$
% \caption{Definition of And-Or tree}
% \label{fig:tree-ty}
% \end{figure}

An And-Or tree is a stratified, variable-width tree, defined as follows:
%
$$
\begin{array}{ll}
\TREE := & \mathrm{Top} \mid \mathrm{Bot} \mid \mathrm{Unexplored}\ \ATOM \\
&  \mid \mathrm{Or}\ (\mathrm{option}\ \TREE)\ \Sigma\ \TREE \mid \mathrm{And}\ \TREE\ (\mathrm{list}\ \ATOM)\ \TREE
\end{array}
$$

It consists of two kinds
of layers that alternate: a disjunctive layer is followed by a conjunctive
layer and vice versa. At the root (a query), the tree records a list of
alternatives (an Or-layer); for each alternative, it records a list of
subqueries to be solved (an And-layer). Intuitively, solving a query requires
solving one alternative in the Or-layer, but all subqueries in the
corresponding And-layer. 
% These invariants about the \TREE data type
% are precisely debscribed in \cref{sec:validtree} where we introduce the
% \vast predicate.


The tree is built incrementally. The \taC{} node
represents an unexplored branch (an atom). When the atom is a
call, we use the backchaining procedure from
\cref{sec:preliminaries} to compute the two layers to attach to the tree:
alternatives form the Or-layer, and the premises of each applicable rule form
the corresponding And-layer. This expansion step is performed by the
\step{} procedure described in \cref{sec:step}.

In
addition to the Unexplored node we have two distinguished nodes, \koC and
\okC, written $\bot$ and $\top$, which represent the smallest failing and
succeeding tree, respectively.

The \orC node, written \(A \lor B_\sigma\), represents the addition of an
alternative \(B_\sigma\) to an existing disjunction $A$. The left subtree
\(A\) is optional and is absent once that branch has been fully explored.
The right alternative is paired with a substitution \(\sigma\), which becomes
the current substitution when backtracking to \(B\). %\marginpar{why option and not Bot?}

The \andC{} node, written \(A \land_{B_0} B\), represents the addition
of a subquery \(B\) to the first choice point in \(A\). We call
\(B_0\) the \emph{reset point} for \(B\): it represents the
continuation for all alternatives of \(A\) except the first. That is,
when backtracking occurs within \(A\), the subtree \(B\) is replaced by
the atoms in \(B_0\). An example is shown in~\cref{sec:na}.


% \begin{figure}
%   \input{tex_code/step_tag.tex}
%   \input{tex_code/step.tex}
% \caption{Step for tree semantics}
% \end{figure}

% \begin{figure}
%   \input{tex_code/next_alt.tex}
% \caption{backtracking operation}
% \end{figure}



\subsection{The semantics}
% \begin{figure}
%     % \input{tex_code/tree_def.tex}
% \begin{coqcode}
% Inductive tag ~$\coloneq$~ Expanded | CutBrothers | Failed | Success.
% Definition step : ~$\mathbb{P}$~ ~$\to$~ ~$\FV$~ ~$\to$~ ~$\Sigma$~ ~$\to$~ tree ~$\to$~ (~$\FV$~ * tag * tree)
% Definition prune : ~$\mathbb{B}$~ ~$\to$~ tree ~$\to$~ option tree
% Inductive runT : ~$\mathbb{P}$~ ~$\to$~  ~$\FV$~ ~$\to$~ ~$\Sigma$~ ~$\to$~ tree ~$\to$~ option (~$\Sigma$~ * option tree) ~$\to$~ ~$\mathbb{B}$~ ~$\to$~ ~$\FV$~ ~$\to$~ Prop
% \end{coqcode}

% \caption{Signatures of the tree-based semantics
% \label{type:steptag}
% \label{type:stepT}
% \label{type:naT}a 
% \label{type:runT}
% \label{fig:signaturesT}
% }
% \end{figure}

% \newcommand{\dingv}{\ensuremath{\square\!\!\!\text{\ding{51}}\;}}
\newcommand{\dingv}{\makebox[0pt][l]{\ensuremath{\square}}\raisebox{.15ex}{\hspace{0.1em}\ensuremath{\checkmark}}}
% \newcommand{\dingx}{\ensuremath{\square\!\!\!\text{\ding{55}}\;}}
\newcommand{\dingx}{\makebox[0pt][l]{\ensuremath{\square}}\raisebox{.0ex}{\hspace{0.01em}\ensuremath{\times}}}
\newcommand{\dings}{\ensuremath{\square\!\!\!\!\cdot\;}}
\newcommand{\dingss}{\ensuremath{\square\!\!\!\cdot\;}}
% \newcommand{\dings}{\makebox[0pt][l]{\ensuremath{\square}}\raisebox{.15ex}{\hspace{0.1em}?}}
% \newcommand{\dings}{{\setlength{\fboxsep}{1pt}\fbox{{\footnotesize ?}}}}

The tree is constructed incrementally by two recursive
functions, \step and \na. Both traverse the tree depth-first, left-to-right.

The type of these two functions is given below.
%
\begin{align*}
  \mathrm{step}  & : \mathbb{P} \to \FV \to \Sigma \to \TREE \to (\FV\times\mathrm{tag}\times\TREE)\\
  \mathrm{prune} & : \mathbb{B} \to \TREE \to \mathrm{option}\ \TREE
\end{align*}
% $$\mathrm{step} : (\mathbb{P}\times\FV\times\Sigma\times\mathrm{tree}) \to (\FV\times\mathrm{tag}\times\mathrm{tree})$$
% $$\mathrm{prune} : (\mathbb{B}\times\mathrm{tree}) \to \mathrm{option\ tree}$$

The step routine, among its outputs, also returns a \emph{tag}, whose definition
is described in the very following section.

To avoid the constraint that all functions must terminate in \rocqP{}, we define the transitive closure
of \step and \na as the inductive relation \run.
More precisely \run iterates calls to \step on incomplete trees to expand
Unexplored nodes. When a tree fails it calls \na to find
the next alternative and continues from there, if one exists.
The node to be explored in a tree is retrieved thanks to \pathend (in~\cref{fig:getend}).
When this node is \okC we say the entire tree is a \emph{success} (short \dingv);
when it is \koC we say the tree \emph{failed} (short \dingx); otherwise the tree is
\emph{incomplete} (short \dingss).


A graphical representation of the execution of the running example
on the tree-based semantics is shown in \cref{tree-ex}. For
compactness, we depict \andC and \orC nodes as $n$-ary,
with children associating to the right. The \koC node acts as the terminating
element of an \orC list, while \okC is the terminating element of an \andC
list. We mark with a black arrow the result of \pathend.


% We detail \step, \na, and \run in \cref{sec:step,sec:next_alt,sec:runT}.


% In \cref{type:stepT,type:naT,type:runT} we give the types
% contrats of these symbols where \fvT is a set of variable names.

% \mvspace{}
% \deftypes[type:steptag]{tex_code/step_tag.tex}
% \deftypes[type:stepT]{tex_code/step_sig.tex}
% \deftypes[type:naT]{tex_code/next_alt.tex}
% \deftypes[type:runT]{tex_code/run_sig.tex}
% \mvspace{}
% A particular tree we want to identify is a \isdead tree (defined in \cref{fig:dead}). This tree has the
% property to never produce a solution: it is eiher the \deadC tree
% or both branches of \orC are dead, or the lhs of \andC is dead. In the 
% latter case, we note that $B$ can be non-dead, but this is not a problem
% since the interpreter can run $B$ only if $A$ is non-dead.

\begin{figure}
    \input{tex_code/next.tex}
    % \caption{Defintion of \getend}
    % \label{fig:getend}
      % \input{tex_code/get_subst.tex}
      % \input{tex_code/path_end.tex}
      % \input{tex_code/succ_path.tex}
      % \input{tex_code/failed_path.tex}
      % \input{tex_code/path_atom.tex}
    \input{tex_code/next_aux.tex}
    % \vspace{20pt}
      % \caption{Functions from \getend}
      % \label{fig:geten/d-aux}
  \caption{The \getend routine and derived functions}
  \label{fig:getend}
\end{figure}

% To support incremental construction, the semantics uses helper functions based
% on \getend{}. Given a tree, \getend{} returns the branch on which
% the execution should focus, following a depth-first, left-to-right traversal
% strategy (see~\cref{fig:getend}).
% If the tree
% is already a leaf, \getend returns its inputs. Otherwise, if the tree is a
% disjunction, the path continues on the left subtree, if it exists, otherwise
% \getend contues on the ths using the substitution stored in the \orC node. In
% the case of a conjunction, if \getend of the lhs returns the tree \okC, then the
% \getend continues in the rhs, otherwise we return the result of the lhs. We
% derive some useful definition from \getend.
% They are listed in\marginpar{fare fresh rules in \cref{tree-ex}}
% \cref{fig:getend-aux}.
% We have pointed by a black arraw the \pathend of 

\begin{figure*}
  \begin{subfigure}{0.99\textwidth}
    \centering
    \resizebox{0.05\textwidth}{!}{%
    \begin{forest}
    for tree={
      l sep=1.5em, s sep=1.5em,
      align=right
    }
    [{{g 0 R}}]
    \end{forest}%
    }
    \caption{Initial query}
    \label{tree-ex0}
  \end{subfigure}
  ~\\
  \begin{subfigure}{0.4\textwidth}
    \resizebox{0.8\textwidth}{!}{%
    \begin{forest}
    for tree={
      l sep=1.5em, s sep=1.5em,
      align=right
    }
    [\orN,orNode
      [\deadN, deadNode,edge=dashed]
      [\andN, andNode,edge label={node[midway,right] {$\sigma_1$}}
        [{{r A B}},name=cur]
        [{{f B C}},edge label={node[midway,right] {$R_1$}}]
        [{{!}},edge label={node[midway,right] {$R_2$}}]
      ]
      [\andN,andNode,edge label={node[midway,right] {$\sigma_2$}}
        [{{f D E}}]
        [{{f E F}}, edge label={node[midway,right] {$R_3$}}]
      ]
    ]
    \curnodeSE{cur};
    \end{forest}%
    }
    \caption{Tree after \step on \eI{g 0 R}}
    \label{tree-ex1}
  \end{subfigure}~~
  \begin{subfigure}{0.5\textwidth}
    \resizebox{0.8\textwidth}{!}{%
    \begin{forest}
    for tree={
      l sep=1.5em, s sep=1.5em,
      align=right
    }
    [\orN,orNode
      [\deadN, deadNode,edge=dashed]
      [\andN, andNode,edge label={node[midway,right] {$\sigma_1$}}
        [\orN,orNode, name=s1
          [\deadN, deadNode,edge=dashed]
          [\okN,okNode,edge label={node[midway,right] {$\sigma_3$}}]
          [\okN,okNode,edge label={node[midway,below] {$\sigma_4$}}]
          [{{!}},edge label={node[midway,right] {$\sigma_5$}}]
        ]
        [{{f B C}},name=cur,edge label={node[midway,right] {$R_1$}}]
        [{{!}},edge label={node[midway,right] {$R_2$}}]
      ]
      [\andN,andNode,edge label={node[midway,right] {$\sigma_2$}}
        [{{f D E}}]
        [{{f E F}},edge label={node[midway,right] {$R_3$}}]
      ]
    ]
    \curnodeSE{cur};
    \end{forest}%
    }
    \caption{\step on \cref{tree-ex1}}
    \label{tree-ex2}
  \end{subfigure}
  ~\\
  \begin{subfigure}{0.49\textwidth}
    \resizebox{0.8\textwidth}{!}{%
    \begin{forest}
    for tree={
      l sep=1.5em, s sep=1.5em,
      align=right
    }
    [\orN,orNode
     [\deadN, deadNode,edge=dashed]
      [\andN, andNode,edge label={node[midway,right] {$\sigma_1$}}
        [\orN,orNode
          [\deadN, deadNode,edge=dashed]
          [\okN,okNode,edge label={node[midway,right] {$\sigma_3$}}]
          [\okN,okNode,edge label={node[midway,below] {$\sigma_4$}}]
          [{{!}},edge label={node[midway,right] {$\sigma_5$}}]
        ]
        [\koN, koNode,name=cur,edge label={node[midway,right] {$R_1$}}]
        [{{!}},edge label={node[midway,right] {$R_2$}}]
      ]
      [\andN,andNode,edge label={node[midway,right] {$\sigma_2$}}
        [{{f D E}}]
        [{{f E F}},edge label={node[midway,right] {$R_3$}}]
      ]
    ]
    \curnodeSE{cur};
    \end{forest}%
    }
    \renewcommand\thesubfigure{d.1}
    \caption{\step on \cref{tree-ex2}}
    \label{tree-ex3}
  \end{subfigure}
  \begin{subfigure}{0.49\textwidth}
    \resizebox{0.8\textwidth}{!}{%
    \begin{forest}
    for tree={
      l sep=1.5em, s sep=1.5em,
      align=right
    }
    [\orN,orNode
     [\deadN, deadNode,edge=dashed]
      [\andN, andNode,edge label={node[midway,right] {$\sigma_1$}}
        [\orN,orNode
          [\deadN, deadNode,edge=dashed]
          [\deadN, deadNode,edge=dashed]
          [\okN,okNode,edge label={node[midway,below] {$\sigma_4$}}]
          [{{!}},edge label={node[midway,right] {$\sigma_5$}}]
        ]
        [{{f B C}},name=cur,edge label={node[midway,right] {$R_1$}}]
        [{{!}},edge label={node[midway,right] {$R_2$}}]
      ]
      [\andN,andNode,edge label={node[midway,right] {$\sigma_2$}}
        [{f D E}]
        [{f E F},edge label={node[midway,right] {$R_3$}}]
      ]
    ]
    \curnodeSE{cur};
    \end{forest}%
    }
    \renewcommand\thesubfigure{d.2}
    \caption{\na on \cref{tree-ex3}}
    \label{tree-ex4}
  \end{subfigure}
  ~\\
  \setcounter{subfigure}{4}
  \begin{subfigure}{0.54\textwidth}
    \resizebox{0.8\textwidth}{!}{%
    \begin{forest}
    for tree={
      l sep=1.5em, s sep=1.5em,
      align=right
    }
    [\orN,orNode
     [\deadN, deadNode,edge=dashed]
      [\andN, andNode,edge label={node[midway,right] {$\sigma_1$}}
        [\orN,orNode
          [\deadN, deadNode,edge=dashed]
          [\deadN, deadNode,edge=dashed]
          [\okN,okNode,edge label={node[midway,below] {$\sigma_4$}}]
          [{{!}},edge label={node[midway,right] {$\sigma_5$}}]
        ]
        [\orN,orNode,edge label={node[midway,right] {$R_1$}}
          [\deadN, deadNode,edge=dashed]
          [\okN,okNode,edge label={node[midway,right] {$\sigma_6$}}]
        ]
        [{{!}},name=cut,edge label={node[midway,right] {$R_2$}}]
      ]
      [\andN,andNode,edge label={node[midway,right] {$\sigma_2$}}
        [{{f D E}}]
        [{{f E F}},edge label={node[midway,right] {$R_3$}}]
      ]
    ]
    \curnodeSE{cut};
    \end{forest}%
    }
    \caption{\step on \cref{tree-ex4}}
    \label{tree-ex5}
  \end{subfigure}~~
  \begin{subfigure}{0.44\textwidth}
    \resizebox{0.8\textwidth}{!}{%
    \begin{forest}
    for tree={
      l sep=1.5em, s sep=1.5em,
      align=right
    }
    [\orN,orNode
     [\deadN, deadNode,edge=dashed]
      [\andN, andNode,edge label={node[midway,right] {$\sigma_1$}}
        [\orN,orNode,name=s1
          [\deadN, deadNode,edge=dashed]
          [\deadN, deadNode,edge=dashed]
          [\okN,okNode,edge label={node[midway,below] {$\sigma_4$}}]
          [\koN, koNode,edge label={node[midway,right] {$\sigma_5$}}]
        ]
        [\orN,orNode,name=s3
          [\deadN, deadNode,edge=dashed]
          [\okN,okNode,edge label={node[midway,right] {$\sigma_6$}}]
        ]
        [\okN,okNode,name=cut]
      ]
      [\koN, koNode,name=s2,edge label={node[midway,right] {$\sigma_2$}}]
    ]
    \path [thick, red, ->] (cut) edge[bend right=30] node[below]{\cutl} (s1);
    \path [thick, red, ->] (cut) edge[bend right=30] node[below]{\cutl} (s3);
    \path [thick, red, ->] (cut) edge[bend right=30] node[right]{\cutr} (s2);
    \curnodeSE{cut};
    \end{forest}%
    }
    \caption{\step on \cref{tree-ex5}}
    \label{tree-ex6}
  \end{subfigure}
  ~\\
\caption{Execution in the tree semantics.
%\footnotemark}
% \footnotetext{
  The substitutions $\sigma_1\dots\sigma_6$ are from \cref{sec:cut-op}.
  $R_1,R_2$, and $R_3$ are reset points and correspond respectively to the
  lists of atoms $[\eI{f B C, !}]$, $[\eI{!}]$, and $[\eI{f E F}]$.
  }
  \label{tree-ex}
  \end{figure*}

\subsection{The \step procedure}\label{sec:step}

The \step procedure takes as input a program, a set of variables, a
substitution, and a tree, and returns an updated set of variables, a
\steptag, and an updated tree.

The set of variables is assumed to contain all variables occurring in the
tree. It is passed to backchain so that rules can be refreshed correctly.
% This guarantees that any call
% to \emph{backchain} returns a list of atoms whose variables are disjoint from
% the variables of the current working tree.\marginpar{not true in figure:fix + also fix in \cref{sec:cut-op}}

The \steptag is implemented as follows: 
%
% \begin{coqcode}
$$\mathrm{tag} := \mathrm{Expanded} \mid \mathrm{CutBrothers} \mid \mathrm{Failed} \mid \mathrm{Success}$$
% \end{coqcode}

It indicates which kind of step was
performed.  Failure and Success are returned for trees that satisfy,
respectively, the \failed and \success tests. 
CutBrothers
indicates the interpretation of a superficial cut: \step was called on an 
And-layer and its \pathend is the cut atom. Finally, Expanded
accounts for the interpretation of predicate calls and non-superficial cuts.

The \step procedure leaves the tree unchanged when it is \emph{success} or
\emph{failed},
%
% \input{tex_code/success_step.tex}
% \input{tex_code/failed_step.tex}
%
while it
expands \taC{} nodes by calling backchain, which returns a list
$l$ of alternatives. If $l$ is empty, then the current call atom is replaced by
\koC. Otherwise, it is replaced by a right-skewed tree of \orC nodes, where
each leaf corresponds to a list of atoms $e \in l$. If $e$ is empty, the leaf
is \okC; otherwise, it is a right-skewed tree of \andC nodes.

\Cref{tree-ex} depicts the tree corresponding to the running example
(\cref{sec:cut-op}) as it undergoes calls to \step and \na.
% , we provide a graphical representation a call to \step on the query
%\marginpar{sottolineare le sostituzioni}
We run query \eI{q 0 R} against the program $P$ in \cref{fig:progx}. Let $c_0$, $c_1$, and $c_2$
be the children of the root. By construction, $c_0$ is None, while
$c_1$ and $c_2$ correspond respectively to the premises of rules $r_1$ and
$r_2$ in $P$. 
% We need the initial \mnone subtree so that we can attach a substitution to $c_1$. 
Note that $c_1$ (resp. $c_2$) is associated with
substitution $\sigma_1$ (resp. $\sigma_2$), whose values are the same as in
\cref{sec:cut-op}. Reset points are defined as follows: %are marked with the $R$ symbol:
$R_1 \coloneq [\eI{f Y Z, !}]$, $R_2 \coloneq [\eI{!}]$, and
$R_3 \coloneq \eI{f Y Z}$. Intuitively, they record the lists of atoms used to
generate the corresponding subtree. Other examples of \step performing a
tree expansion via backchain appear in \cref{tree-ex2,tree-ex3,tree-ex5}.

\subsubsection{The interpretation of a cut}

The step corresponding to a cut performs two actions: (i) the cut node is
replaced by \okC; and (ii) the subtrees in the scope of \cutC are pruned. More
precisely, (ii) prunes the left siblings of the \cutC node (in the same
And-layer, denoted \cutl) and prunes the right uncles of \cutC (in the
one-level-up Or-layer, denoted \cutr).

% \begin{definition}[\logo{}Left-siblings (resp. right-sibling) and Right-uncles]
%   Given a node $A$, the left-siblings (resp. right-sibling) of $A$
%   are the list of subtrees sharing the same parent of $A$ and that appear
%   on its left (resp. right). The right-uncles of $A$ are the list 
%   of right-sibling of the father of $A$.
% \end{definition}

% \begin{definition}[\logo{}Hard-kill, \cutr and Soft-kill, \cutl]
%   Given a tree $t$, hard-kill replaces the given subtree with the \koC node.
%   If $t$ is a success-tree, then soft-kill replaces with \koC all subtrees
%   that are not part of the path in $t$ leading to the \okC node.
% \end{definition}


An example of the impact of cut is shown in \cref{tree-ex6}.
% In \cref{tree-ex5}, we see that the \pathend is a cut. 
% The cut is replaced by the \okC node and it need to cut-away some alternatives in the tree. 
The red arrows
indicate the scope of the cut and are labeled with \cutr and \cutl to indicate
which pruning operation is applied to each pointed subtree. Note that
the evaluation of a cut keeps the path leading to the cut node unchanged, in
the sense that substitution $\sigma_4$ is preserved, while the path under
substitution $\sigma_5$ (which also succeeds) is replaced by \koC. This
amounts to discarding the third rule of predicate \eI{r}.
% (see \cref{sec:cut-op}).

\subsection{The \na procedure}\label{sec:next_alt}
\label{sec:na}

When backchain returns an empty list, \step produces a tree that \emph{failed}
and the computation must backtrack. The \na procedure is
responsible for producing the new tree from which the computation can
continue.
% The \step procedure alone is not sufficient to reproduce entirely the behavior
% of the full expected prolog interpreter. As proved in \cref{lemma:success} and
% \cref{lemma:failed}, \step does not make any progression on the tree being
% interpreted. To solve this problem, we need a routine capable of backtracking in
% case of failures. This routine return the backtracked tree if it exists,
% \mnone otherwise.

In \cref{tree-ex3} we encounter a failure because no rule applies to the atom
\eI{f B C} under substitution $\sigma_3$, which maps \eI{B} to \eI{0}. The
\na procedure prunes the path leading to this failure and resets the current
sub-query to its original shape. More precisely, it kills the \okC node under
$\sigma_3$ (since that branch has been fully explored and produced no
solution), and then replaces the \koC node with the information stored in the
reset point $R_1$. This restores the call \eI{f B C}, which can then be
reconsidered under substitution $\sigma_4$.

Furthermore, \na serves another important purpose. Its behavior depends on the
boolean flag it receives as input: \na false $t$ recursively removes
all failures (if any) from $t$, whereas \na true $t$ removes the
first success in $t$. For example, the tree in \cref{tree-ex6} contains a
successful path that maps \eI{R} to \eI{2}. Calling \na\ on this tree returns
None, since there are no alternatives in the tree after the success.


% The interpreter should return this sucess, but, due to
% the ``non-deterministic'' behavior of the prolog system, we also need to return
% all the other solution that could be available by evaluating the rest of the
% tree. After the replacement of the \pathend in \cref{tree-ex6} with \koC, we
% remain with a tree having all failing path. The \na procedure keeps removing
% them until discovering that the tree have no worth-to explore paths, therefore
% it will return \mnone.


% In particular, 
% we need to bracktrack on failures. Moreover, in case of success,
% we should return a tree cleaned of its success,
% this is essential to, non deterministically, find the next
% solution of a given query.
% By \cref{lemma:success,lemma:failed}, we know that \step returns the identity
% on successful and failed trees. In order
% to continue the computation on these particular trees, we
% need the \na procedure aiming to expecially work with
% failed and successful trees:
% and its implementation in \cref{fig:na}.

% The \na procedure takes a boolean and a tree, clean it from failures
% or success and returns a new tree if this tree still contains
% a non explored path. The idea behind \na is to 
% clean recursively every subtree in DFS order if its \pathend is
% a failure. Moreover, if the boolean passed to \na is true, then it
% erases the first successful path in the tree.

% The base cases of \na are immidiate.
% The \orC case is rathere intuitive: if the lhs of the \orC does
% not exist we look for the \na in the rhs. Otherwise, we look for the \na
% in the lhs, if this \na does not exists, we look for the \na in the rhs.

% We want to spend few words about the \andC case, since the reset point $B0$
% for $B$ plays an important role. 
% The \na in an \andC tree should consider two cases: if the lhs succeeds, then
% the \na should be retrived in the rhs. If this alternative does not exists
% it means that the rhs has entirely been explored. We need to erase the success
% in the lhs and try to find if a non-explored alternative exists. If so,
% we return a new tree with the new lhs and the rhs is built from the reset point.
% \cI{big_and} is a trivial function build a right-skewed tree of and nodes
% where the leaves are the atoms written in the reset point.
% We need to reuse the reset point since,
% the step procedure in \andC trees
% evaluates the rhs of a \andC tree if the lhs succeeds.
% This evaluation is dependent on the substitution in the lhs tree.
% Therefore, if we need to backtrack in the lhs, we need to reset the
% rhs.

% Some interesting properties of \na are shown below; they illustrate how \na
% complements \step with respect to failed, successful, and incomplete trees.

% \mvspace{}
% \begin{lemma}[\logo{}incomplete\_prune\_id]\label{th:incompletepruneid}
%   $\forall b\ t, \mathrm{incomplete}\ t \to \mathrm{prune}\ b\ t = t$
%   % \mathrm{~\}nsuremath{\forall}~ ~\ensuremath{b}~ ~\ensuremath{t}~, incomplete ~\ensuremath{t}~ ~\ensuremath{\to}~ prune ~\ensuremath{b}~ ~\ensuremath{t}~ = ~\msome~~\ensuremath{t}~.}
% \end{lemma}

% \begin{lemma}[\logo{}prune\_Some]\label{th:pruneSome}
%   $\forall b\ t\ t', \mathrm{prune}\ b\ t = t \to \mathrm{failed}\ t' = \bot$
% \end{lemma}

% \begin{lemma}[\logo{}prune\_None]\label{th:pruneNone}
%   $\forall t\ t', \mathrm{prune}\ \bot\ t = \bot \to \mathrm{failed}\ t' = \top$
% \end{lemma}


% \input{tex_code/incpl_prune.tex}
% \input{tex_code/na_failed.tex}
% \input{tex_code/naNfail.tex}
% \mvspace{}


% For example, in \cref{tree-ex1-exp} the step procedure has created a failed
% tree: its path-end ends in \koC. The expected behavior of \na is to take
% this \koC node and make it a .... This allows \step to continue the
% exploration of the tree. In particular, the path-end of this new tree ends
% in \okC. The step leaves the tree unchanged producing the new substitution.\marginpar{subst taken form the or}
% This solution however is not unique, we should be able to backtrack on this
% successful tree. To do so we can call \na and it will deadify the \okC node
% allowing \step to proceed on r X Z.

% \begin{figure}
%   \input{tex_code/next_alt_code.tex}
%   \caption{\na implementation}
%   \label{fig:na}
% \end{figure}

\subsection{The \run relation and its properties}\label{sec:runT}

The inductive relation \run has the following type:
%
$$\run : \mathbb{P} \to \FV \to \Sigma \to \mathbb{T} \to \mathrm{sol} \to \mathbb{B} \to \FV \to \cI{Prop}$$

It associates a program, a set of variables, an initial substitution $\sigma$, and a
tree $t$ with a solution $r$, a boolean $b$, and an updated set of variable
names $v'$.

The type of sol is defined as follows:
%
$$\mathrm{sol} := \mathrm{Zero} \mid \mathrm{One}\ \Sigma \mid \mathrm{Many}\ \Sigma\ \TREE$$

Zero represents a failing execution. One\ $\sigma$ represents an interpretation
producing exactly one solution, i.e. no backtracking is possible from the given
state. It also carries the substitution $\sigma$ that makes the interpretation
succeed. Finally, Many $\sigma\ t$ represents an execution in which the
original state produces the solution $\sigma$, while $t$ represents the
remaining unexplored alternatives.

The boolean $b$ records whether a superficial cut was evaluated during execution,
and $v'$ is the updated set of variables tracking freshly generated names.


\begin{figure*}[!hbt]
\begin{subfigure}{\textwidth}
\captionsetup{labelformat=xxbracket}
\begin{center}
\small
\begin{prooftree}
	\AxiomC{$\text{\dingv\ t} \quad \mathrm{next\_subst}\ \sigma\ t \arr \sigma'  \qquad \mathrm{prune}\ \mathrm{true}\ t \arr \mathrm{None} $}
  \RightLabel{$\rulelabel{\stopot} $}
  \UnaryInfC{$\run\ \prog\ v\ \sigma\ t \arr (\mathrm{One}\ \sigma', \mathrm{false}, v)$}
\end{prooftree}
\begin{bprooftree}
  \AxiomC{
    \ensuremath{
  %{\mathrm{success}\ t} \quad
  {\text{\dingv}\ t} \quad
  {\mathrm{next\_subst}\ \sigma\ t \arr \sigma' } \quad
  {\mathrm{prune}\ \mathrm{true}\ t \arr \mathrm{Some}\ t' }
  }}
  \RightLabel{\rulelabel{\stopt}}
  \UnaryInfC{\ensuremath{\run\ \prog\ v\ \sigma\ t \arr (\mathrm{Many}\ (\sigma', t'), \mathrm{false}, v)}}
\end{bprooftree}
\begin{bprooftree}
  \AxiomC{\ensuremath{
      %\mathrm{incomplete}\ t \quad 
      \dings\ t \quad 
      \mathrm{step}\ \prog\ v\ \sigma\ t \arr (v', tg, t') \quad 
      \run\ \prog\ v'\ \sigma\ t' \arr (r, b, v'')}}
  \RightLabel{\rulelabel{\stept}}
  \UnaryInfC{\ensuremath{\run\ \prog\ v\ \sigma\ t\ \arr (r, (tg = \mathrm{CutBrothers}) \lor b, v'')}}
\end{bprooftree}
~\\\vspace{0.5em}
\begin{bprooftree}
  \AxiomC{\ensuremath{
  % {\mathrm{failed}\ t} \quad
  {\text{\dingx}\ t} \quad
  {\mathrm{prune}\ \mathrm{false}\ t \arr \mathrm{Some}\ t'} \quad
  {\run\ \prog\ v\ \sigma\ t' \arr r} } }
  \RightLabel{\rulelabel{\backt}}
  \UnaryInfC{\ensuremath{\run\ \prog\ v\ \sigma\ t \arr r}}
\end{bprooftree}
\begin{bprooftree}
  \AxiomC{\ensuremath{\mathrm{prune}\ \mathrm{false}\ t \arr \mathrm{None}}}
  \RightLabel{\rulelabel{\failt}}
  \UnaryInfC{\ensuremath{\run\ \prog\ v\ \sigma\ t \arr (\mathrm{None}, \mathrm{false}, v)}}
\end{bprooftree}
\end{center}

% \begin{prooftree}
  %   \AxiomC{$\success\ A$}
  %   \RightLabel{$\run\_done$}
  %   \UnaryInfC{$\run\ fv\ s_1\ A\ (\getsubst\ s_1\ A)\ (\na\ \top\ A)$}
  % \end{prooftree}

  % \begin{prooftree}
  %   \AxiomC{$\incomplete\ A$}
  %   \AxiomC{$\step\ u\ p\ fv_0\ s_1\ A\ = (fv_1,\ st,\ B)$}
  %   \AxiomC{$\run\ fv_1\ s_1\ B\ s_2\ r$}
  %   \RightLabel{$\run\_step$}
  %   \TrinaryInfC{$\run\ fv_0\ s_1\ A\ s_2\ r$}
  % \end{prooftree}

  % \begin{prooftree}
  %   \AxiomC{$\failed\ A$}
  %   \AxiomC{$\na\ \bot\ A = \mathrm{Some}\ B$}
  %   \AxiomC{$\run\ fv_0\ s1\ B\ s_2\ r$}
  %   \RightLabel{$\run\_fail$}
  %   \TrinaryInfC{$\run\ fv_0\ s_1\ A\ s_2\ r$}
  % \end{prooftree}  

  \caption{Rule system for \run}
  \label{fig:run-rules}
\end{subfigure}
~\\\vspace{1em}
\begin{subfigure}{\textwidth}
\captionsetup{labelformat=xxbracket}
\begin{center}
\small
\begin{bprooftree}
  \AxiomC{$A \quad (A ~\cuts)$}
  \RightLabel{$(\lor_{il})$}
  \UnaryInfC{$A \lor B $}
\end{bprooftree}\hspace{1em}
%
\begin{bprooftree}
  \AxiomC{$\lnot A \quad (A ~\cuts)$}
  \RightLabel{$(\lor_{ir1})$}
  \UnaryInfC{$\lnot (A \lor B) $}
\end{bprooftree}\hspace{1em}
%
\begin{bprooftree}
  \AxiomC{$\lnot A$}
  \AxiomC{$B \quad (A ~\mathrm{does~not~cut})$}
  \RightLabel{$(\lor_{ir2})$}
  \BinaryInfC{$A \lor B$}
\end{bprooftree}
\\
\begin{bprooftree}
  \AxiomC{$A \lor B$}
  \AxiomC{[A]}
  \noLine
  \UnaryInfC{$C$}
  \AxiomC{$(A ~\cuts)$}
  \RightLabel{$(\lor_{e1})$}
  \TrinaryInfC{$C$}
\end{bprooftree}\hspace{1em}
%
\begin{bprooftree}
  \AxiomC{$A \lor B$}
  \AxiomC{[A] }
  \noLine
  \UnaryInfC{$C$}
  \AxiomC{$[\lnot A, B] \phantom{\quad (A ~\mathrm{does~not~cut})}$}
  \noLine
  \UnaryInfC{$C \quad (A ~\mathrm{does~not~cut})$} 
  \RightLabel{$(\lor_{e2})$}
  \TrinaryInfC{C}
\end{bprooftree}
\end{center}
\caption{Introduction and elimination rules for disjunction ( \Cref{th:orintroleft,th:orintroright,th:orintroright2,th:orelim1,th:orelim2} )\label{fig:logic}}
\end{subfigure}

\end{figure*}

The \run predicate has five cases, depicted as inference rules in \cref{fig:run-rules}.

% where we separate the first four arguments and the last one with a squiggle arrow,
% evocative of the fact that the given set of rules is deterministic (\cref{th:runTdet}).


Rules \stopot and \stopt say that when the tree $t$ is a success, the
substitution $\sigma'$ is extracted from $t$ (starting from $\sigma$); depending
on the result of \na true $t$ -- which cleans the tree of its successful
path -- we return One or Many, according to whether alternatives remain
to be explored. Rule \stept applies when \pathend\ $t$ is an atom: \step is invoked to
evaluate $t$, and \run is called recursively on the updated tree. When \pathend\ $t$ is a failure, rule \backt calls \na
to clear the failed path; if the resulting cleaned tree exists, \run is
called recursively on it. Finally, rule \failt accounts for trees with no
solutions.


\subsubsection{Properties of \run}
\label{sec:run-prop}


% \input{tex_code/runT_det.tex}

% The proof is by induction on the first \run derivation and is immediate,
% because the rules are selected based on \pathend\ $t$. In particular, the
% hypotheses \success $t$, \incomplete $t$, and \failed $t$ are
% mutually exclusive. Moreover, by \cref{th:pruneNone}, the hypothesis of
% \failt implies that $t$ \emph{failed}; hence \failt is exclusive
% with \stopot, \stopt and \stept, and it is also exclusive with
% \backt since they impose different requirements on the output of \na.

The boolean output of \run allows us to state interesting properties about the
\orC node in the presence of cut (\cref{fig:logic}).
To simplify their presentation, we introduce the
following notation for \orC nodes. When the left-hand side is $\mathrm{None}$,
we write the symbol $\square$. Otherwise, the notation $A \lor B_\sigma$
implicitly denotes $(\mathrm{Some}\ A) \lor B_\sigma$.

% \input{tex_code/runSST_or.tex}
\begin{theorem}[\logo{}or\_intro\_left]\label{th:orintroleft}%
	$\forall\ p\ v\ v'\ \sigma\ \sigma'\ A\ A'\ \sigma_1\ B ,$\\
  $\mphant
  \run\ p\ v\ \sigma\ A\ ( \mathrm{Many}\ \sigma'\ A' )\ \mathrm{true}\ v' \to
  $\\
	$\mphant\mphant
  \run\ p\ v\ \sigma\ ( A \lor B\ \phantom{!}_{\!\!\!\!\sigma_1} )\ ( \mathrm{Many}\ \sigma'\ ( A' \lor \mathrm{Bot}\ \phantom{!}_{\!\!\!\!\sigma_1} ) )\ \mathrm{false}\ v'
  $
\end{theorem}
% \input{tex_code/runNT_or.tex}
\begin{theorem}[\logo{}or\_intro\_right]\label{th:orintroright}%
  $\forall\ p\ v\ v'\ \sigma\ A\ \sigma_1\ B ,$\\
$ 
 \mphant
 \run\ p\ v\ \sigma\ A\ \mathrm{Zero}\ \mathrm{true}\ v' \to 
 \run\ p\ v\ \sigma\ ( A \lor B\ \phantom{!}_{\!\!\!\!\sigma_1} )\ \mathrm{Zero}\ \mathrm{false}\ v'
$
\end{theorem}
% \input{tex_code/runNF_or.tex}
\begin{theorem}[\logo{}or\_intro\_right2]\label{th:orintroright2}%
	$\forall\ p\ v_0\ v_1\ v_2\ \sigma\ A\ \sigma_1\ \sigma_2\ B\ b ,$\\
$\mphant
	\run\ p\ v_0\ \sigma\ A\ \mathrm{Zero}\ \mathrm{false}\ v_1 \land 
  \run\ p\ v_1\ \sigma_1\ B\ ( \mathrm{One}\ \sigma_2 )\ b\ v_2 \to
$\\
$\mphant\mphant
	\run\ p\ v_0\ \sigma\ ( A \lor B\ \phantom{!}_{\!\!\!\!\sigma_1} )\ ( \mathrm{One}\ \sigma_2 )\ \mathrm{false}\ v_2 
$
\end{theorem}
% $$$$

\Cref{th:orintroleft,th:orintroright,th:orintroright2} correspond to the introduction rules
for disjunction. If $A$ executes a cut, one \emph{cannot} obtain $A \lor B$ from
$B$: the execution of $A$, whether it ultimately succeeds or fails, makes the
success of $B$ irrelevant (the cut discards it). In the first lemma, $B$ is
forced to be \koC; in the second one, the failure of $A$ absorbs $B$. The last
lemma is connected to the depth-first exploration of the tree: proving a
disjunction from its right-hand side can only occur after the left-hand side
has been disproved.
% %
% \begin{center}  

% \begin{bprooftree}
%   \AxiomC{$A \quad (A ~\cuts)$}
%   \RightLabel{$(\lor_{il})$}
%   \UnaryInfC{$A \lor B $}
% \end{bprooftree}
% \begin{bprooftree}
%   \AxiomC{$\lnot A \quad (A ~\cuts)$}
%   \RightLabel{$(\lor_{ir1})$}
%   \UnaryInfC{$\lnot (A \lor B) $}
% \end{bprooftree}

% \vspace{18pt}

% \begin{bprooftree}
%   \AxiomC{$\lnot A$}
%   \AxiomC{$B \quad (A ~\mathrm{does~not~cut})$}
%   \RightLabel{$(\lor_{ir2})$}
%   \BinaryInfC{$A \lor B$}
% \end{bprooftree}

% \end{center}

\Cref{th:orelim1,th:orelim2} are elimination
rules for disjunction: 

% \input{tex_code/run_orSST.tex}
\begin{theorem}[\logo{}or\_elim1]\label{th:orelim1}$\forall\ p\ v\ v'\ \sigma\ \sigma'\ \sigma_1\ A\ A'\ B\ B' ,$\\
$
	\phantom{xx}\run\ p\ v\ \sigma\ ( A \lor B\ \phantom{!}_{\!\!\!\!\sigma_1} )\ ( \mathrm{Many}\ \sigma'\ ( A' \lor B'\ \phantom{!}_{\!\!\!\!\sigma_1} ) )\ \mathrm{false}\ v' \to
$\\
$ \phantom{xx}\phantom{xx}
	\exists\ b ,\ \run\ p\ v\ \sigma\ A\ ( \mathrm{Many}\ \sigma'\ A' )\ b\ v' \land\ B' = \mathrm{if}\ b\ \mathrm{then}\ \bot\ \mathrm{else}\ B
$
\end{theorem}

%\input{tex_code/run_orSNT.tex}
\begin{theorem}[\logo{}or\_elim2]\label{th:orelim2}
  $\forall\ p\ v\ v'\ \sigma\ \sigma'\ \sigma_1\ A\ B\ B' ,$\\
$\phantom{xx}
	\run\ p\ v\ \sigma\ ( A \lor B\ \phantom{!}_{\!\!\!\!\sigma_1} )\ ( \mathrm{Many}\ \sigma'\ ( \square\ \lor B'\ \phantom{!}_{\!\!\!\!\sigma_1} ) )\ \mathrm{false}\ v' \to
$
$\phantom{xx}\phantom{xx}
	( \run\ p\ v\ \sigma\ A\ ( \mathrm{One}\ \sigma' )\ \mathrm{false}\ v' )\ \lor
$\\
$\phantom{xx}\phantom{xx}
	( \exists\ v_2\ b ,\ \run\ p\ v\ \sigma\ A\ \mathrm{Zero}\ \mathrm{false}\ v_2 \land
$\\
$\phantom{xx}\phantom{xx}\phantom{xx}\phantom{xx}
  \run\ p\ v_2\ \sigma_1\ B\ ( \mathrm{Many}\ \sigma'\ B' )\ b\ v' ) 
$
\end{theorem}



From $A \lor B$, when $A$ is not inert (i.e. not already explored), we cannot
deduce $A$ or $B$ independently. Instead, we can only derive a weakened
conclusion of the form $\top \lor B'$, where $B'$ provides no information in the
case where the exploration of $A$ performs a cut. This yields an elimination
rule that is stronger than the usual one.
If $A$ does not cut, the elimination rule has the usual two premises, together
with the additional constraint that whenever $B$ holds, $A$ must have failed.
This again reflects the depth-first traversal of the proof tree.

% $$
% \begin{center}  
% \begin{bprooftree}
%   \AxiomC{$A \lor B$}
%   \AxiomC{[A]}
%   \noLine
%   \UnaryInfC{$C$}
%   \AxiomC{$(A ~\cuts)$}
%   \RightLabel{$(\lor_{e1})$}
%   \TrinaryInfC{$C$}
% \end{bprooftree}

% \vspace{18pt}

% \begin{bprooftree}
%   \AxiomC{$A \lor B$}
%   \AxiomC{[A] }
%   \noLine
%   \UnaryInfC{$C$}
%   \AxiomC{$[\lnot A, B] \phantom{\quad (A ~\mathrm{does~not~cut})}$}
%   \noLine
%   \UnaryInfC{$C \quad (A ~\mathrm{does~not~cut})$} 
%   \RightLabel{$(\lor_{e2})$}
%   \TrinaryInfC{C}
% \end{bprooftree}
% % $$
% \end{center}

It is worth recalling that the ``$A$ cuts'' side-condition in the rules above is
precisely the boolean value returned by \run. Without this information, it is
impossible to formulate these rules: it is not merely a matter of whether $A$
contains a syntactic occurrence of cut, but whether that cut is actually
executed during the (dis)proof of $A$ (in rules $(\lor_{ir1})$ and $(\lor_{ir2})$).
Indeed, an atom preceding the cut may fail, so the cut is never reached.

As anticipated in the introduction, these lemmas show that, in the presence
of cut, disjunction is not commutative.




\subsection{Inadequacy of the stack-based semantics for formal reasoning}

The lemmas that we presented in
\cref{sec:run-prop} are hard to express in the stack-based semantics, due to the
lack of structure in the stack to delimit the scope of the cut operator. For
example, consider a stack $a_0, a_1, \dots, a_n$. If $a_0$ succeeds but executes a
cut, it is hard to relate the remaining alternatives $a_1, \dots, a_n$ to the
alternatives $b_1, \dots, b_m$ that survive the cut, since the only simple
invariant is that $b_1, \dots, b_m$ is a suffix of $a_1, \dots, a_n$. If
$a_1, \dots, a_n$ were generated before the call whose execution generates the cut
in $a_0$, then they all stay; but if some were generated afterwards, they are
discarded. Expressing this precisely would require attaching, at the very least,
time-stamps (or depths) to goals, which amounts to a cumbersome encoding of a
tree.




\newsavebox{\elpiboxx}

\begin{figure*}[!htb]
    \begin{lrbox}{\elpiboxx}

    % \includegraphics[width=0.8\textwidth, keepaspectratio]{src/t2lex.pdf}
    \begin{minipage}[t][][t]{9cm}
\begin{forest}
for tree={
  % minimum height=5em,
  l sep=1.5em, s sep=1.5em,
  align=right
}
[\orN,orNode
  [\deadN, deadNode,edge=dashed]
  [\andN, andNode,edge label={node[midway,right] {$\sigma_1$}}
    [\orN,orNode, name=s1
      [\deadN, deadNode,edge=dashed]
      [\subnode{ss1}{\okN},okNode,edge label={node[midway,right] {$\sigma_3$}}]
      [\subnode{ss2}{\okN},okNode,edge label={node[midway,below] {$\sigma_4$}}]
      [\subnode{ss3}{!},edge label={node[midway,right] {$\sigma_5$}}]
    ]
    [{{f B C}},name=cur,edge label={node[midway,right] {$R_1$}}]
    [{{!}},edge label={node[midway,right] {$R_2$}}]
  ]
  [\andN,name=X,andNode,edge label={node[midway,right] {$\sigma_2$}}
    [{{f D E}},name=Y]
    [{{f E F}},edge label={node[midway,right] {$R_3$}},name=Z]
  ]
] 
\curnodeNE{cur};
\node[draw,red,fit=(X)(Y)(Z)]{\subnode{ss4}{}};
\end{forest}
\end{minipage}
%
\begin{minipage}[t][7.5cm][t]{12cm}
  \vspace{60pt}
\begin{tikzpicture}[baseline]

\matrix (outer) [matrix of nodes,
                 nodes={draw, minimum width=0.5cm, minimum height=1cm, anchor=center},
                 column sep=0.3cm, row sep=0.3cm] {
\phantom{AAAAAAAA} & \phantom{AAAAAAAA} & \phantom{AAAAAAAAAAA} & \phantom{AAAAAAAAA} &  \node[draw=none] (A) {\phantom{xx}};\\
};

\matrix (inner1) [matrix of nodes,
                 nodes={minimum size=0.6cm},
                 column sep=0.1cm, row sep=0.1cm] at (outer-1-1.center) {
\subnode{ss11}{f B C}, & ! \\
};

\matrix (inner2) [matrix of nodes,
                 nodes={minimum size=0.6cm},
                 column sep=0.1cm, row sep=0.1cm] at (outer-1-2.center) {
\subnode{ss22}{f B C}, & ! \\
};

\matrix (inner3) [matrix of nodes,
                 nodes={minimum size=0.6cm},
                 column sep=0.1cm, row sep=0.1cm] at (outer-1-3.center) {
\subnode{ss33}{!}, & f B C, & ! \\
};

\matrix (inner4) [matrix of nodes,
                 nodes={minimum size=0.6cm},
                 column sep=0.1cm, row sep=0.1cm] at (outer-1-4.center) {
\subnode{ss44}{f D E}, & f E F \\
};


\matrix (innerL) [matrix of nodes,
                 nodes={minimum size=0.6cm},
                 column sep=0.1cm, row sep=0.1cm] at (A.center) {
\phantom{x} \\
};

\draw[->, thick]
  ($(inner1-1-2.north)+(0,0.3)$) -- ++(0,0.6)  
  -- ($(innerL-1-1.north)+(0,0.9)$)            
  -- ($(innerL-1-1.north)+(0,0.3)$);           

\draw[->, thick]
  ($(inner2-1-2.north)+(0,0.3)$) -- ++(0,0.5)  
  -- ($(innerL-1-1.north)+(0,0.8)$)            
  -- ($(innerL-1-1.north)+(0,0.3)$);           

\draw[->, thick]
  ($(inner3-1-3.north)+(0,0.3)$) -- ++(0,0.4)  
  -- ($(innerL-1-1.north)+(0,0.7)$)            
  -- ($(innerL-1-1.north)+(0,0.3)$);           

\draw[->, thick]
  ($(inner3-1-1.south)-(0,0.3)$) -- ++(0,-0.4)  
  -- ($(inner4-1-1.south)-(0,0.7)$)            
  -- ($(inner4-1-1.south)-(0,0.3)$);           

\node at ($(outer-1-1.north west)+(-0,0.2)$) {$\sigma_3$};
\node at ($(outer-1-2.north west)+(-0,0.2)$) {$\sigma_4$};
\node at ($(outer-1-3.north west)+(-0,0.2)$) {$\sigma_5$};
\node at ($(outer-1-4.north west)+(-0,0.2)$) {$\sigma_2$};
\end{tikzpicture}
\end{minipage}%

\newcommand{\marr}[3]{\draw[->, red, dashed, line width=1pt] ($(#1)+(0,-0.5+#3)$) to[in=270, out=270] ($(#2)+(-0.4,-0.7)$)}

\begin{tikzpicture}[overlay,remember picture]
\marr{ss1}{ss11}{0};
\marr{ss2}{ss22}{0};
\marr{ss3}{ss33}{0};
\marr{ss4}{ss44}{-0.9};
\end{tikzpicture}

    \end{lrbox}

  \begin{center}
    \resizebox{.8\textwidth}{!}{\usebox{\elpiboxx}}
  \end{center}
\caption{Example of \ttl execution\label{fig:cc}}
\end{figure*}
\section{Equivalence of the two semantics}

Before proving the equivalence of the two semantics, we first need
to relate trees to stacks.
% In the following subsection we define the
% function \ttl, which is in charge of translating trees to
% stacks.

\subsection{From trees to stacks}\label{sec:t2l}

% \marginpar{t2l should be t2s or t2s}

% The \ttl recursive function translates a tree to a stack.
% we give junst the implementation of the
% base cases, the full implementation is available at \href{https://github.com/FissoreD/elpi-formalization/blob/ho-check-ffun/theories/t2l.v}{\emph{this}} link.

The function \ttl takes as input the tree $t_0$, a substitution
$\sigma$ for the input tree, and an auxiliary list of choice points $cp$.
%
$$
\mathrm{\ttl} : \TREE \to \Sigma \to \mathrm{alts} \to \mathrm{alts}
$$

These choice points
represent alternatives that appear at the same level of the current
tree, such as the right siblings of an \orC{} node.

The \okC node represents one successful alternative; therefore it is translated
into a singleton list whose only element has an empty list of goals. The \koC
node represents failure, hence it is mapped to the empty list of alternatives.
Finally, atoms are mapped to a singleton list containing one alternative whose
only goal is that atom, with an empty cut-to list.
At this stage, the cut-to list cannot point to $cp$: atoms are not
right-uncle subtrees, whereas $cp$ represents alternatives that will actually
be discarded if the atom turns out to be a cut. The correct cut-to list is
therefore determined later, once the trees corresponding to sibling subtrees
have been inspected.

The translation of $A \lor B_\sigma$ creates two lists of alternatives,
one for $A$ and the other for $B$. The alternatives of $B$ are valid
choice points for $A$ and are therefore passed to the translation of $A$.
The two lists of alternatives can then be concatenated. Moreover, every atom
occurring one level below $cp$ (i.e. in $A$ or $B$) must add $cp$ to its
cut-to alternatives.

The translation of $A \land_{B_0} B$ starts with the translation of $A$. If the
translation of $A$ is an empty list, we return the empty list of
alternatives without even inspecting $B$ or $B_0$, since an empty
translation of $A$ indicates failure, which in turn implies the
failure of the entire conjunction. When the translation of $A$ is
a list $(\sigma_0, g) :: a$, the $B$ node represents the continuation of the
first alternative $g$, whereas $B_0$ is the continuation for each of the
remaining alternatives in $a$.
% While performing these translation, we need to carefully update the cut-atlernatives.

The function \ttl is not injective: distinct trees can translate to the
same stack. In particular, a transition from a tree $t_1$ to a tree $t_2$
performed by \run may leave both trees mapped to the same
stack. We refer to such a transition as a \emph{silent transition}.

Notably, the stack semantics has no notion of a failed stack. Therefore, when
proving the equivalence between the two semantics, we must take into account
that \run may perform more iterations than \nur before returning a solution or
reporting a failure. We illustrate this non-injective behavior with a concrete
example below.


\subsubsection{Example of \ttl execution}
The translations of the trees in \cref{tree-ex} are shown in \cref{ex-elpi}.
The letters in the figures relate each tree to the corresponding stack.
For example, \ref{tree-ex1} is translated into \ref{ex-elpi1}. We also
note that \ref{tree-ex3} and \ref{tree-ex4} are both translated into
\ref{ex-elpi3}, confirming that \ttl is not injective: different trees can map to
the same stack. Consequently, some transitions performed by \run are not visible
in \nur.
%
As an example of a call to \ttl, we consider the tree and the stack in~\cref{fig:cc},
taken respectively from \cref{tree-ex2,ex-elpi2}. The red arrows
point from the head of an alternative in the tree to the head of the
corresponding cell in the stack.
We focus on the deepest \orC{} node in the tree. This subtree
is a disjunction with four children. The first is ignored, since it
is None. The success node below $\sigma_3$ has \eI{f B C} and the cut
operator as continuation, corresponding to the first cell in the stack.
We note that the cut operator points outside the
stack, since the scope of this cut eliminates its right uncle. The
success node below $\sigma_4$ has $R_1$ as continuation, where $R_1$ is
the list of goals \eI{f B C, !}; this is translated into the second cell
of the stack. Finally, the cut operator under
$\sigma_5$ also has $R_1$ as continuation; it corresponds to the third
alternative. We can see that the first cut points to
the translation of the subtree under $\sigma_2$, since that subtree is one
\orC-layer above its right uncles.

\subsection{Equivalence}
\label{sec:equiv}

The equivalence we are interested in states that a query produces the
same substitution and alternatives under both semantics.

The tree-based semantics exposes more information than this comparison
requires, so we hide the surplus behind existential quantifiers.

\begin{definition}[\logo{}\runo]\label{th:runo}%
	$\lambda\ p\ v\ \sigma\ t\ r .$\\
  $\phantom{xx}\exists\ b\ v' ,\ \run\ p\ v\ \sigma\ (\mathrm{Unexplored}\ t)\ r\ b\ v' $
\end{definition}

For each possible outcome, we prove the equivalence of the two semantics.

% \input{tex_code/runS_to_runTCZero.tex}
\begin{corollary}[\logo{}equiv\_zero]\label{th:runStorunTZ}%
	$\forall\ p\ t\ v,\ t \subseteq v \to$\\
  $
	\phantom{xx}
	\nur\ p\ v\ [\varepsilon ,\ [\mathrm{call}\ t,\ \mnil]]\ \mathrm{None} \leftrightarrow
  $\\
  $
	\phantom{xx}
  \mathrm{\runo}\ p\ v\ \varepsilon\ ( \mathrm{call}\ t  )\ \mathrm{Zero} 
  $
\end{corollary}

% \input{tex_code/runS_to_runTCOne.tex}
\begin{corollary}[\logo{}equiv\_one]\label{th:runStorunTO}%
	$\forall\ p\ t\ \sigma'\ v,\ t \subseteq v \to$\\
  $\phantom{xx}
	\nur\ p\ v\ [\varepsilon ,\ [\mathrm{call}\ t,\ \mnil]]\ ( \mathrm{Some}\ ( \sigma' ,\ \mnil ) ) \leftrightarrow
  $\\
	$\phantom{xx}
  \mathrm{\runo}\ p\ v\ \varepsilon\ ( \mathrm{call}\ t  )\ ( \mathrm{One}\ \sigma' ) 
  $
\end{corollary}

% \input{tex_code/runS_to_runTCMany1.tex}
\begin{corollary}[\logo{}sound\_many]\label{th:runStorunTM1}%
	$\forall\ p\ t \sigma'\ t'\ v,\ t \subseteq v \to$\\
  $\phantom{xx}
	\mathrm{\runo}\ p\ v\ \varepsilon\ ( \mathrm{call}\ t  )\ ( \mathrm{Many}\ \sigma'\ t' ) \to
  $\\
  $\phantom{xx}
	\nur\ p\ v\ [\varepsilon ,\ [\mathrm{call}\ t,\ \mnil]]\ ( \mathrm{Some}\ ( \sigma' ,\ \mathrm{t2s}\ t'\ \sigma\ \mnil ) )
  $ 
\end{corollary}

% \input{tex_code/runS_to_runTCMany2.tex}
\begin{corollary}[\logo{}complete\_many]\label{th:runStorunTM2}%
	$\forall\ p\ t\ \sigma'\ x\ \mathrm{xs}\ v,\ t \subseteq v \to$\\
  $\phantom{xx}
  \nur\ p\ v\ [\varepsilon ,\ [\mathrm{call}\ t,\ \mnil]]\ ( \mathrm{Some}\ ( \sigma' ,\ x\ \mathrm{::}\ \mathrm{xs} ) ) \to
  $\\
	$\phantom{xx}
  \exists\ t' ,\ \mathrm{\runo}\ p\ v\ \varepsilon\ ( \mathrm{call}\ t  )\ ( \mathrm{Many}\ \sigma'\ t' ) \land
  \mathrm{t2s}\ t'\ \varepsilon\ \mnil = x\ \mathrm{::}\ \mathrm{xs}
  $
\end{corollary}

Because both semantics rely on the same notion of substitution,
\cref{th:runStorunTZ,th:runStorunTO} can be stated without relating the
states — the stacks and trees — on which the two semantics operate. In
contrast, in \cref{th:runStorunTM1,th:runStorunTM2} the execution leaves
choice points open, so we do need to relate the two kinds of states.

Following the proof technique of~\cite{yves-equiv}, we only need the mapping
\ttl from trees to stacks, already described in \cref{sec:t2l}; the converse
mapping is never constructed explicitly. The corollaries above follow from
the two theorems proved in \cref{sec:eqproof}.


% are more
% interesting. Although they do not provide a double implication, we claim that
% together they complete the equivalence we need.

% The former theorem states that the interpretation of $\mathrm{\runo}$ on $t$
% returns alternatives $t'$. In the conclusion of the theorem, we certify the
% stack returned by runS is exactly the translation $t'$ to a stack (via the
% t2s function). 

% In the converse direction, $\nur$ launched on the tree $t$ returns the
% stack $x :: xs$. Since we have no function for converting a stack back into a
% tree, we postulate the existence of a tree $t'$, which the result of running
% \runo on t, and prove that converting $t'$ to a stack produces exactly the list
% $x :: xs$.





% In order to relate the two semantics we have to relate the computations states,
% i.e., the And-Or tree and the stack of alternatives. We define a translation from trees to stacks, called \ttl, but
% we do not explicitly provide the converse translation, an economy
% allowed by the proof technique we employ, inspired by~\cite{yves-equiv}.

\subsection{Equivalence proof}\label{sec:eqproof}


\begin{wrapfigure}{r}{.15\textwidth}
  \centering
  \resizebox{1.2\linewidth}{!}{%
  \begin{tikzpicture}[]
      \node (t1) at (0,0) {$t$};
      \node (t2) [right=1.5cm of t1]  {$t'$};
      \node (e1) [below=1.5cm of t1]  {$a$};
      \node (e2) [right=1.5cm of e1]  {$a'$};
      % letf to right arrows
      \draw [->] (t1) -- (t2) node[midway, above]{\run\ $t$};
      \draw [->] (e1) -- (e2) node[midway, below]{\nur\ $a$};

      % up to down arrows
      \draw [->] (t1) -- (e1);
      \draw [->] (t2) -- (e2);
      \node[rotate=-90, anchor=center] at ($(t1)!0.5!(e1) + (-0.35,0)$) {t2s};
      \node[rotate=-90, anchor=center] at ($(t2)!0.5!(e2) + (0.35,0)$) {t2s};

      % diagonal arrow
      \path[->] (e1) edge (t2);
  \end{tikzpicture}%
  }
\end{wrapfigure}

The two theorems below each proceed by induction on the execution of one
semantics — the top or the bottom horizontal arrow in the diagram — to
derive the other. We begin with the top-to-bottom direction,
from trees to stacks.

Since not every tree or stack is reachable from the initial state under
either semantics, it is important to identify the subset of valid
trees. We defer the exact definition of \vast{} to \cref{sec:validtree}.

% \input{tex_code/tree_to_elpi.tex}
\begin{theorem}[\logo{}runT2S]\label{th:runTtorunS}%
  \begingroup\small
	$\forall\ p\ t\ \sigma\ r\ v,\ t\ \cup\ \sigma \subseteq v \to$\\
  $\phantom{xx}
	\mathrm{sld\_tree}\ t \land (\exists\ b\ v',\ \run\ p\ v\ \sigma\ t\ r\ b\ v') \to \\
  $
  $\phantom{xx}
	\begin{cases}
	\nur\ p\ v\ ( \mathrm{t2s}\ t\ \sigma\ \mnil )\ \mathrm{None} & \mathrm{if}\ r = \mathrm{Zero}\\
	\nur\ p\ v\ ( \mathrm{t2s}\ t\ \sigma\ \mnil )\ (\mathrm{Some}\ (\sigma', \mnil)) & \mathrm{if}\ r = \mathrm{One}\ \sigma'\\
	\nur\ p\ v\ ( \mathrm{t2s}\ t\ \sigma\ \mnil )\ (\mathrm{Some}\ ( \sigma' ,\ \mathrm{t2s}\ t'\ \sigma\ \mnil)) & \mathrm{if}\ r = \mathrm{Many}\ \sigma'\ t'\\
	\end{cases}
  $
  \endgroup
\end{theorem}

\begin{proof} By induction on $\run$. \end{proof}

The converse direction could in principle be proved symmetrically, by
defining a mapping from stacks to trees together with a predicate
identifying the valid stacks that the semantics can generate from the
initial one. The backward simulation technique of~\cite{yves-equiv} that we
follow makes both definitions unnecessary, by using an existential
quantifier instead (on $t'$ in the third case). Validity of the stack $a$
is recovered for free, since it is obtained from a valid SLD tree; and by
inhabiting the existential in a constructive logic, we implicitly obtain
the mapping from stacks to trees.

% \input{tex_code/elpi_to_tree.tex}
\begin{theorem}[\logo{}runS2T]\label{th:runStorunT}%
  \begingroup\small
	$\forall\ p\ v\ a\ r\ v\ \sigma\ t,\ t\ \cup\ \sigma \subseteq v \to$\\
  $\phantom{xx}\nur\ p\ v\ a\ r \to
	\ \mathrm{sld\_tree}\ t \to \mathrm{t2s}\ t\ \sigma\ \mnil = a \to \exists \ b\ v',
  $\\
	% \mathrm{match}\ r\ \mathrm{with} \\
  $\phantom{xx}
  \begin{cases}
    \run\ p\ v\ \sigma\ t\ \mathrm{Zero}\ b\ v'             & \mathrm{if}\ r = \mathrm{None} \\
    \run\ p\ v\ \sigma\ t\ ( \mathrm{One}\ \sigma' )\ b\ v' & \mathrm{if}\ r = \mathrm{Some}\ ( \sigma' ,\ \mnil ) \\
    \exists\ t',\ \run\ p\ v\ \sigma\ t\ ( \mathrm{Many}\ \sigma'\ t' )\ b\ v' 
        & \mathrm{if}\ r =  \mathrm{Some}\ ( \sigma' ,\ a' ) \\
    \phantom{xxxx}\land\ \mathrm{t2s}\ t'\ \sigma\ \mnil = a'     &  \\
  \end{cases}
  $
  \endgroup
\end{theorem}

\begin{proof} 
By induction on \nur accounting for silent transitions as we align
its iterations with \run.\end{proof}

% The proof is based on the idea explained in~\cite[Section
% 3.5.1]{yves-equiv} and depicted in the figure on the right:
% we have a tree $t$ whose translation is $a$, we proceed by
% induction on the execution of \nur. This gives us not only the output $a'$ but
% that hypothesis that it exists a tree $t'$ such that its translation to a
% stack is $a'$.\qed %This proof strategy is indirectly providing a kind of \ltt function by combining the execution of the \nur and \ttl.

% Thanks to the two functions \vast and \ttl, we are able to finally state the
% equivalence theorems between the semantics. The proof we are giving are on a
% slightly different definition of \run, since in the equivalence, we ignore the
% verification for the boolean and the variable set returned by \run, these piece
% of data are paricularly meant for the proof in \cref{sec:run-prop}.

% The definition we use is the following:

% \begin{definition}\label{th:runo}
%   $\mathrm{\runo}\ \prog\ v\ \sigma\ t\ r := \exists\ b\ v',\ \run\ \prog\ v\ \sigma\ t\ r\ b\ v'$
% \end{definition}

% Proving the four theorems above as their are claimed is not possible in Rocq,
% since the induction principle would be too strong. We need therefore to prove a
% more generic main theorem talking about the interpretation of generic trees and
% stacks, related via t2s, instead of call to term. Morever, in order
% to make this proof work, the trees we take into account must be satisfy the
% sld\_tree predicate which restrict the theorem only to those valid trees that
% by invariant are generated during the evaluation of the \runo inductive.
















% Before describing the translation of trees to stacks (in~\cref{sec:t2l})
% we need to define the notion of \emph{valid SLD tree}. The {tree}
% datatype indeed contains trees that cannot be generated
% by {runT} via {step} or {prune}, for example
% all the trees that do not correspond to a depth-first left-to-right
% tree construction strategy. 


% In the previous section we have introduces the two semantics, they are 
% The equivalence between the two semantics is possible under two conditions: we
% need to work with ``valid tree'', i.e. all of those trees that can be
% generated from a call. Secondly, we need to translate trees into elpi
% states. In the next to subsection we propose the two functions \vast and \ttl.

\subsubsection{Valid SLD trees}\label{sec:validtree}
\begin{figure}[b]
    \input{tex_code/sld_tree.tex}
    \caption{The sld\_tree predicate\label{fig:vt} }
\end{figure}


The class of valid SLD trees is captured by the \vast
function shown in \cref{fig:vt}.
While all base cases of \tree{} are valid, we need to analyze the \orC and
\andC constructors more carefully.
% \marginpar{TODO: explain better valid tree as requested by reviewr, maybe make a few examples}

For the \orC constructor, we distinguish two cases depending on whether the
left-hand side is present. If it is not, the right-hand side must be a
valid SLD tree. Otherwise, the left-hand side must be a valid SLD tree, and the
right-hand side must either be the \koC tree or be unexplored. The former case
arises when a superficial cut in the left-hand side cuts away the
right-hand side.
In the latter case, we use the \emph{base\_or} predicate
to check that $B$ is the right-skewed tree formed by a disjunction of
conjunctions generated after a successful backchain for a call.

For the \andC constructor, the left-hand side is required to be a valid SLD tree.
The shape of the right-hand side depends on whether the left-hand side has
succeeded. If it has not, the right-hand side must not be
explored, i.e. $B$ must be
the right-skewed tree containing conjunctions of premise atoms.
This is enforced by the \emph{big\_and} procedure, which generates a tree
from the reset point $B_0$ (the same procedure used to
transform each alternative output by backchain into the And-layer
of the resulting tree).
When the left-hand side has succeeded, the right-hand side
may have been explored, and must therefore be a valid SLD tree.

In the mechanization we show that \vast is preserved by both
\step and \na.
% out the two main invariants stating that valid SLD trees
% are preserved by calls to \step and \na.

% % \input{tex_code/valid_tree_step.tex}
% \begin{lemma}[\logo{}sld\_tree\_step]\label{th:validtreestep}%
% 	$\forall\ \sigma\ v\ A\ r ,$\\
%   $\phantom{xx}\mathrm{sld\_tree}\ A \to \mathrm{step}\ u\ p\ v\ \sigma\ A = r \to \mathrm{sld\_tree}\ (\mathrm{snd}\ r)$
% \end{lemma}

% % \input{tex_code/valid_tree_prune.tex}
% \begin{lemma}[\logo{}sld\_tree\_prune]\label{th:validtreeprune}%
% 	$\forall\ A\ B\ b ,$\\
%   $\phantom{xx}\mathrm{sld\_tree}\ A \to \mathrm{prune}\ b\ A = \mathrm{Some}\ B \to \mathrm{sld\_tree}\ B $
% \end{lemma}

% % We can therefore derive how valid trees are preserved by the stack interpreter.

% % \input{tex_code/valid_tree_run.tex}
% \begin{theorem}[\logo{}sld\_tree\_run]\label{th:validtreerun}%
% 	$\forall\ b\ \sigma\ \sigma'\ v\ v'\ A\ B,$\\
%   $\phantom{xx}\mathrm{sld\_tree}\ A \to \run\ u\ p\ v\ \sigma\ A\ ( \mathrm{Many}\ \sigma'\ B )\ b\ v' \to \mathrm{sld\_tree}\ B $
% \end{theorem}

%


% \begin{figure}  
%     \begin{coqcode}
%       Fixpoint t2l ~$t$~ ~$\sigma$~ (cp: alts) : alts ~$\coloneq$~
%       match ~$t$~ with
%       | Top     ~$\Rightarrow$~ [(~$\sigma$~, ~\mnil\unskip~)]
%       | Bot     ~$\Rightarrow$~ ~\mnil~
%       | TA a   ~$\Rightarrow$~ [(~$\sigma$~, [(a, ~\mnil\unskip~)])]
%       ...
%     \end{coqcode}
%   \caption{Tree to stack \label{fig:ttl} }
% \end{figure}
% \marginpar{TODO: put all code}
% \begin{table}
%   \centering
%   \begin{tabular}{c|c|c|c|c|c|c}
%     % \toprule
%     $t$ & \ref{tree-ex0} & \ref{tree-ex1} & \ref{tree-ex2} & \ref{tree-ex3} and \ref{tree-ex4} & \ref{tree-ex5} & \ref{tree-ex6} \\
%     $t2l\ t\ \epsilon\ \mnil$ & \ref{ex-elpi0} & \ref{ex-elpi1} & \ref{ex-elpi2} & \ref{ex-elpi3} & \ref{ex-elpi4} & \ref{ex-elpi5}
%     % \bottomrule
%   \end{tabular}
%   \caption{Tree to list from \cref{tree-ex} and \cref{ex-elpi}}
%   \label{tab:t2l}
% \end{table}

% The translation of a tree to a list is performed by shown in \cref{fig:ttl}. It takes the
% tree to be translated, a substitution (called $s$), a list of alternatives, called
% \emph{bt}. The substitution $s$ tells what is the substitution of the 
% alternative we are building and is updated when going to the
% rhs of the \orC constructor. The \emph{bt} list represents the future 
% alternatives list that have the same depth of the current tree root. 
% They are useful to know if they should be added to the current goal as cut alternatives.

% An \okC node represent a success in a tree, then the translation of this tree
% returns a singleton list with the cuple $(s, \mnil)$: there is one alternative
% with $0$ goals, i.e. a success in elpi by \stops. The \koC node
% represent a failure, and, therefore $0$ alternatives. An atom is translated into
% a singleton with the atom as first goal and the empty cut-alternative list: we
% are not adding $bt$ since they are the alternatives for the right-sibling, not
% the right-uncles, this means that if we have $a$ is a cut, then it erases the
% alternatives bt.

% In a disjunction, we are scendendo di un livello the distance from our
% backtracking list. This means that $bt$ becomes the list of right-uncles of the
% two branches of the \orC constructor. The compilation of the rhs is done
% independently of if the lhs exists: we transform the rhs into an elpi state and
% passing the empty list of alternatives, since the rhs as no right-siblings.
% Then, thanks to \texttt{add\_ca\_deep}, we recursively add $bt$ to every
% cut-alternative appearing in the translated goals. If the lhs exists, we
% translate it and pass the translation of the rhs as the list of right-siblings.
% Then we prepend the translated lhs to the translated rhs and add $bt$ with
% \texttt{add\_ca\_deep}.

% We compile the \andC case by translating its lhs to the list $lA$ and reset
% point to the list $lB0$. Then, thanks to \texttt{add\_deep}, we recrusively
% append $lB0$ to the alternatives born in $lA$, i.e. we leave unchanged the
% pointers to $bt$, if any, in $lA$. We finally need to treat cases whenever
% the list $lA$ is empty or not. In the former case the empty list is returned,
% otherwise, we hasve a list $[::(s_0,gs)\&al]$. By the semantics of
% the \andC in the \run interpreter, the rhs of the \andC represent the
% sequent of goals to be executed after the first alternative in the lhs,\marginpar{alternative in tree? rephrase}
% it corresponds to the list of goals $gs$.
% The reset point $B0$ is to be appended to the next alternatives in the lhs,
% that is the queue of $lA$, that is $al$.

% We note therefore that the compilation of $lB$ is done by passing the
% alternatives $al ++ bt$ since the alternatives in $al$ are to be\dots



% \newcommand{\thte}{\texttt{tree\_to\_elpi}} % theorem tree to elpi name
% \newcommand{\thet}{\texttt{elpi\_to\_tree}} % theorem elpi to tree name

% From \cref{tree-ex,ex-elpi}, we observe that, upon backtracking, \run may perform
% more steps than \nur before returning a solution or a failure.
% These silent transitions must be skipped
% when proving the equivalence between the two semantics. Therefore, we
% prove the following lemma.

% \begin{lemma}[\logo{}pruneF\_t2s]\label{th:pruneFtreetostack}%
% \begin{multline*}
% 	\forall\ t\ t'\ b ,\ \mathrm{sld\_tree}\ t \to \mathrm{failed}\ t \to \mathrm{prune}\ b\ t = \mathrm{Some}\ t' \to \\
% 	\forall\ l\ \sigma ,\ \mathrm{t2s}\ t\ \sigma\ l = \mathrm{t2s}\ t'\ \sigma\ l 
% \end{multline*}
% \end{lemma}

% % \input{tex_code/pruneF_tree_to_stack.tex}

% \begin{proof}By induction on the tree $t$. %\marginpar{dire di $v$}
% \end{proof}



% The first direction of the equivalence states that the execution of a
% valid SLD tree is matched by the execution of the stack obtained by
% translating the tree. Moreover, the unexplored alternatives returned as
% a tree correspond to those returned as a stack.

% % \input{tex_code/tree_to_elpi.tex}
% \begin{theorem}[runT\_to\_runS]\label{th:runTtorunS}%
% \begin{multline*}
% 	\forall\ p\ t\ \sigma\ r ,\ \mathrm{let}\ v\ \mathrm{:=}\ \mathrm{vars}\ t\ \cup\ \mathrm{vars}\ \sigma\ \mathrm{in}\ 
% 	\mathrm{sld\_tree}\ t \to \mathrm{\runo}\ p\ v\ \sigma\ t\ r \to \\
% 	\begin{cases}
% 	\nur\ p\ v\ ( \mathrm{t2s}\ t\ \sigma\ \mnil )\ \mathrm{None} & \mathrm{if}\ r = \mathrm{Zero}\\
% 	\nur\ p\ v\ ( \mathrm{t2s}\ t\ \sigma\ \mnil )\ (\mathrm{Some}\ (\sigma', \mnil)) & \mathrm{if}\ r = \mathrm{One}\ \sigma'\\
% 	\nur\ p\ v\ ( \mathrm{t2s}\ t\ \sigma\ \mnil )\ (\mathrm{Some}\ ( \sigma' ,\ \mathrm{t2s}\ t'\ \sigma\ \mnil)) & \mathrm{if}\ r = \mathrm{Many}\ \sigma'\ t'\\
% 	\end{cases}
% \end{multline*}
% \end{theorem}

% % \begin{proof}
% % We proceed by induction on the execution of \run'. There are five cases
% % to consider. The first (similar) cases arises from \stopot and \stopt; they deal with
% % successful trees. A successful tree corresponds to a stack that
% % start with an empty list, in both cases
% % we conclude using \stops. The case for \stept
% % requires treating two subcases: \step evaluates either a cut or a call.
% % Accordingly, we apply \cuts or \calls, followed by the
% % induction hypothesis. The case for \backt is silent: it represents
% % the non-injective behavior of \ttl; however, thanks to
% % \cref{th:pruneFtreetostack} and the induction hypothesis, the conclusion is
% % proved. The last case arises from the derivation \failt. It is
% % easily proved using \fails and the fact that if \na returns
% % None when trying to erase failure in $t$, then \ttl applied to $t$
% % returns the empty list.
% % \end{proof}

% The converse direction is stated following~\cite{yves-equiv}: instead of
% using a function to translate a stack into a tree it states that any
% tree that translates to the given stack has an equivalent execution,
% i.e. gives the same solution and returns an unexplored tree that
% translates to the unexplored stack.
% % \input{tex_code/elpi_to_tree.tex}
% \begin{theorem}[runS\_to\_runT]\label{th:runStorunT}%
% \begin{multline*}
% 	\forall\ p\ v\ a\ r ,\ \nur\ p\ v\ a\ r \to
% 	\forall\ \sigma\ t ,\ \mathrm{sld\_tree}\ t \to \mathrm{t2s}\ t\ \sigma\ \mnil = a \to \\
% 	% \mathrm{match}\ r\ \mathrm{with} \\
%   \begin{cases}
%     \mathrm{\runo}\ p\ v\ \sigma\ t\ \mathrm{Zero}             & \mathrm{if}\ r = \mathrm{None} \\
%     \mathrm{\runo}\ p\ v\ \sigma\ t\ ( \mathrm{One}\ \sigma' ) & \mathrm{if}\ r = \mathrm{Some}\ ( \sigma' ,\ \mnil ) \\
%     \exists\ t' ,\ \mathrm{\runo}\ p\ v\ \sigma\ t\ ( \mathrm{Many}\ \sigma'\ t' ) 
%     \land\ \mathrm{t2s}\ t'\ \sigma\ \mnil = a'     & \mathrm{if}\ r =  \mathrm{Some}\ ( \sigma' ,\ a' ) \\
%   \end{cases}
% \end{multline*}
% \end{theorem}

% \noindent\hskip\labelsep{\itshape Proof.}\ignorespaces

% \begin{wrapfigure}{r}{.15\textwidth}
%   \centering
%   \resizebox{\linewidth}{!}{%
%   \begin{tikzpicture}[]
%       \node (t1) at (0,0) {$t$};
%       \node (t2) [right=2cm of t1]  {$t'$};
%       \node (e1) [below=2cm of t1]  {$a$};
%       \node (e2) [right=2cm of e1]  {$a'$};
%       % letf to right arrows
%       \draw [->] (t1) -- (t2) node[midway, above]{\run'\ $t$};
%       \draw [->] (e1) -- (e2) node[midway, below]{\nur\ $a$};

%       % up to down arrows
%       \draw [->] (t1) -- (e1);
%       \draw [->] (t2) -- (e2);
%       \node[rotate=-90, anchor=center] at ($(t1)!0.5!(e1) + (-0.35,0)$) {t2s};
%       \node[rotate=-90, anchor=center] at ($(t2)!0.5!(e2) + (0.35,0)$) {t2s};

%       % diagonal arrow
%       \path[->] (e1) edge (t2);
%   \end{tikzpicture}%
%   }
% \end{wrapfigure}

% The proof is based on the idea explained in~\cite[Section
% 3.5.1]{yves-equiv} and depicted in the figure on the right:
% we have a tree $t$ whose translation is $a$, we proceed by
% induction on the execution of \nur. This gives us not only the output $a'$ but
% that hypothesis that it exists a tree $t'$ such that its translation to a
% stack is $a'$.\qed %This proof strategy is indirectly providing a kind of \ltt function by combining the execution of the \nur and \ttl.

% \vspace{10pt}

% Stating the converse direction of \cref{th:runTtorunS} in a symmetric way
% would require a function
% \ltt transforming a stack $a$ into a tree $t$.
% Writing this function is far from trivial, since the structure of
% the tree is lost by \backchainS and \nur that
% intuitively keep the state as a big disjunction of conjunctions by
% distributing conjunctions over disjunctions. Moreover, one would need to
% define a notion of valid stack that is equally intricate.

% The constructive nature of the logic of Rocq tells us that the proof above
% provides an effective function translating stacks into trees. However, that
% function also uses an execution trace of the stack-based semantics, from which
% it recovers the missing tree structure.

\section{Case study: determinacy analysis}\label{sec:determinacy}

As an application of the tree semantics, we mechanize the soundness proof
of the determinacy-checking algorithm introduced in version~3 of the
\elpiP{} language~\cite{fissore2026}. The static checker takes as input
the signatures of the predicates declared by the user and verifies that
the rules of a deterministic predicate generate at most one solution; we
call this kind of predicate a function. Thanks to this static
verification, the user can better control unexpected backtracking.

A predicate behaves deterministically if two conditions are
satisfied: \emph{mutual exclusion} guarantees that at most one rule can
be successfully applied to any given query, whereas \emph{rule-body checking}
ensures that each rule does not create choice points, for example by
calling non-deterministic predicates.

% Determinacy checking is strongly related to the modes of predicates: we
% differentiate between input and output arguments. A deterministic call relates
% to its inputs only one output.
% In the literature, we find several works on
% determinacy checking, namely~\cite{1996Somogy,king2005,1989Warren}. These works
% need a mode checker that statically ensures that, in a query with ground inputs,
% each recursive call is performed with ground inputs.

\subsection{Unification and matching}

Our mechanization implements the unification algorithm by
Martelli and Montanari~\cite{10.1145/357162.357169}; we prove it
terminating (using the standard lexicographic ordering), correct, and
complete.

% The first notion to provide is the application of an acyclic substitution:

% \begin{definition}[\logo{}deref]
%   $$
%   \begin{array}{rlr}
%     \sigma\ (\mathrm{Var}\ n) &= t &\mathrm{if}\ (n,t) \in \sigma\\
%     \sigma\ (\mathrm{Var}\ n) &= \mathrm{Var}\ n& \mathrm{otherwise}\\
%     \sigma\ (\mathrm{Symb}\ n) &= \mathrm{Symb}\ n \\
%     \sigma\ (\mathrm{Pred}\ n) &= \mathrm{Pred}\ n \\
%     \sigma\ (\mathrm{App}\ t_1\ t_2) &= \mathrm{App}\ (\sigma\ t_1)\ (\sigma\ t_2)
%   \end{array}
%   $$
% \end{definition}

\begin{definition}[\logo{}acyclic]
  $\lambda \sigma. \mathrm{dom}\ \sigma \cap \mathrm{codom}\ \sigma = \varnothing$
\end{definition}

Note that this definition of acyclicity entails that the substitution is
necessarily self-applied, i.e. $\sigma \coloneq \{ X \mapsto f~Y, Y \mapsto a\}$
is not acyclic, whereas the equivalent $\sigma' \coloneq \{ X \mapsto f~a, Y \mapsto a\}$
is.
The algorithm by Martelli and Montanari generates substitutions that
are acyclic in this sense.

% \input{tex_code/unif_correct.tex}

\begin{definition}[\logo{}renaming]
An injective and acyclic mapping from variables to variables
\end{definition}

\begin{definition}[\logo{}mgu] $\lambda\ \sigma\ t_1\ t_2.$\\
  $\phantom{xx}\sigma\ t_1 = \sigma\ t_2 \land (\forall \sigma', \mathrm{acyclic}\ \sigma' \land \sigma'\ t_1 = \sigma'\ t_2 \to$\\
    $\phantom{xx}\phantom{xx}\exists r : \mathrm{renaming},
        \forall t, r\ (\sigma'\ (\sigma\ t)) = r\ (\sigma'\ t))$
\end{definition}

\begin{lemma}[\logo{}unify\_correct]\label{th:unifycorrect}%
$\forall\ t_1\ t_2\ \sigma,$\\
$\phantom{xx}\mathrm{unify}\ t_1\ t_2\ \varepsilon = \mathrm{Some}\ \sigma \to \mathrm{mgu}\ \sigma\ t_1\ t_2 $
\end{lemma}

% \input{tex_code/unify_complete.tex}
\begin{lemma}[\logo{}unify\_complete]\label{th:unifycomplete} $\forall\ t_1\ t_2,$\\
$( \exists\ \sigma ,\ \mathrm{acyclic}\ \sigma \land\ \sigma\ t_1 = \sigma\ t_2 ) \to
  \exists\ \sigma' ,\ \mathrm{unify}\ t_1\ t_2\ \varepsilon = \mathrm{Some}\ \sigma'$
\end{lemma}

The \elpiP language adopts a restricted unification routine for input arguments,
called \matching~\cite{1991ait-wam,zhou2012}. Like \unify, \matching takes a
query term $t_1$ and a pattern $t_2$, but it also takes a set of frozen
variables $v$ and returns an optional substitution $\sigma'$ that does not
assign any variable in $v$.

To address this need, we extend the Martelli-Montanari unification
procedure so that it takes a set of frozen variables.
The function \matching has the following property:

% \input{tex_code/matching_disj.tex}
\begin{lemma}[\logo{}matching\_disj]\label{um}\label{th:matchingdisj} $\forall\ v\ t_1\ t_2\ \sigma\ \sigma', \mathrm{acyclic}\ \sigma \to$\\
  $\phantom{xxx} 
  \sigma\ t_2 \ \subseteq\ {v} \to
  \mathrm{matching}\ v\ t_1\ t_2\ \sigma = \mathrm{Some}\ \sigma' \to \sigma'\ t_1 = \sigma\ t_2$
\end{lemma}

% In particular, we can see how the returned substitution $\sigma'$, differently
% from \cref{th:unifycorrect}, does not affect variables in $\sigma\ t_2$.

% \begin{corollary}[\logo{}matching\_same]\label{umm}\label{th:matchingsame} $\forall t\ q\ \sigma\ \sigma',$\\
%   $\phantom{xxx} \mathrm{acyclic}\ \sigma \to \mathrm{matching}\ t\ q\ \sigma = \mathrm{Some}\ \sigma' \to \sigma'\ q = \sigma\ q$
% \end{corollary}


% \marginpar{The statement abouve does not say that $\sigma'\ t_2 = t_2$. Did you prove it?}

% If the variables (\cI{vars}) of two terms are disjoint ($\#$) and the variables in the support of $\sigma$ are
% disjoint from the variables in $t_1$, then, if matching succeeds, only the
% variables in $t_2$ are assigned; that is, $t_1 = \sigma'~t_2$, and the
% variables in $t_1$ are not assigned in $\sigma'$.


The backchain function uses matching or unify on the arguments $q$
depending on whether the argument is in \emph{input} or
\emph{output} mode.
% We assume two important properties of our unification routines.

For the static checker, and in particular to validate rule mutual
exclusion, we need to ensure that no two rules can be used on the same query in
the absence of cut. We exploit the behavior of matching on input
arguments to guarantee this property: mutual exclusion ensures that every
pair of rules for a deterministic predicate has an input term that does
not unify.

An important property we derive from the matching and unification
procedures is the following. Given three pairwise variable-disjoint terms
$h_1$, $h_2$, and $q$, representing two rule heads in the database and a
query, if both heads match the query $q$, then the two heads must unify.
% This is formalized in the following lemma.

% \input{tex_code/matching_unify_transP.tex}
\begin{lemma}[\logo{}matching\_unify\_trans]\label{th:matchingunifytransP}%
	$\forall\ v\ h_1\ h_2\ q,$\\
  $\phantom{xx}h_1 \cap h_2 = \varnothing \land h_1 \cap q = \varnothing \land h_2 \cap q = \varnothing \land q \subseteq v \to$ \\
	$\phantom{xx}\phantom{xx}\mathrm{matching}\ v\ h_1\ q\ \varnothing \land \mathrm{matching}\ v\ h_2\ q\ \varnothing \to \mathrm{unify}\ h_1\ h_2\ \varnothing $
\end{lemma}

% \begin{proof}
%   By \cref{um}, we know that the matching of $h_1$ (resp. $h_2$) and $q$ gives a
%   substitution $\sigma_1$ (resp. $\sigma_2$) where the variables in $q$ are not
%   assigned, i.e. its domain only contains variables in $h_1$ (resp $h_2$). Using
%   \cref{th:unifycomplete} we need to prove that it exists a
%   substitution $\sigma'$ such that $\sigma'\ h_1 = \sigma'\ h_2$. This
%   substitution is the composition of $\sigma_1$ and $\sigma_2$, that is
%   $\sigma' = \lambda x. \sigma_1 (\sigma_2\ x)$. Since $h_1$ and
%   $h_2$ are disjoint, so are the domains of $\sigma_1$ and $\sigma_2$, therefore
%   $\sigma' h_1 = \sigma_2 (\sigma_1\ h_1)$ and $\sigma' h_2 = \sigma_2 (\sigma_1\ h_2)$.
%   We can now conclude the proof: $\sigma'\ h_1 = \sigma_2 (\sigma_1\ h_1) = \sigma_2\ q = q = \sigma_2\ h_2 = \sigma' h_2$.
% \end{proof}


% \input{tex_code/unif_trans.tex}
% \input{tex_code/matchunif.tex}

Finally, we also need to prove that the refreshing of variables
performed at runtime does not affect unification.
In particular, a refreshing renaming $f$ for a term $t$ is a function from
variables to variables such that: (i) $f$ is defined on all variables in
$t$, i.e. every variable is renamed; and (ii) $f$ is injective, i.e. it
does not identify distinct variables. Refreshing therefore produces
$\alpha$-equivalent terms.
%\marginpar{spiegare disjoint in hyp of lemma 17}

% Below, we define
% \emph{refresh\_for} to test the validity of a refreshing renaming.
% \Cref{th:unifren} states the property of term unifiability with respect to term
% renaming. That is, if the renamings of terms $t_1$ and $t_2$ are unifiable
% under two respective valid refreshing renamings, and they have disjoint
% variables, then for any pair of renamings $z$ and $x$ that are valid renamings
% for $t_1$ and $t_2$, respectively, and are disjoint from each other,
% unification also succeeds. In other words, this theorem accounts for
% $\alpha$-equivalence of terms and the unification of terms with disjoint
% variables.

% \input{tex_code/refresh_for.tex}

% The refreshing performed at runtime ensures that 

\begin{definition}[\logo{}$=_\alpha$]
  $\lambda t_1\ t_2.$ \\
  $\phantom{xxx}
  \exists (r:\mathrm{renaming\_for}\ t_2), t_1 = \mathrm{rename}\ r\ t_2$
\end{definition}

% \input{tex_code/unif_ren.tex}
\begin{lemma}[\logo{}unif\_ren\_ac]\label{th:unifren}
  $\forall\ t_1\ t_2\ t_1'\ t_2',$\\
  $\phantom{xx}t_1 =_\alpha t_1' \land\ t_2 =_\alpha t_2' \land t_1 \cap t_2 = \varnothing \land\ t_1' \cap t_2' = \varnothing \to$\\
  $\phantom{xx}\phantom{xx}
  \mathrm{unify}\ t_1\ t_2\ \varepsilon \to \mathrm{unify}\ t_1'\ t_2'\ \varepsilon
  $
\end{lemma}

\subsection{Soundness of determinacy checking}

In \cref{fig:progx}, the rules of the program are equipped with three
signatures, one per predicate. Besides the arity of each predicate, a
signature specifies which arguments are inputs and which are outputs,
their types, and whether the predicate is deterministic. The signatures
indicate that \texttt{f} and \texttt{g} are expected to be functions, as specified
by the \eI{~\PYG{k+kd}{func}~} keyword, whereas \eI{r} is a relation, as
indicated by the \eI{~\PYG{k+kd}{pred}~} keyword. The
\eI{~\PYG{k+kMode}{\PYGZhy{}\PYGZgt{}}~} symbol separates inputs from
outputs; therefore, all three predicates are expected to take an
\texttt{int} as input and return an \texttt{int} as output.

The signatures of the program are stored in the \texttt{sig} field of
the program and are encoded as described in
\cite{fissore2026}: we distinguish data from predicates, and predicates
are classified as deterministic or non-deterministic.

A program execution behaves deterministically if, given a program, a
substitution, and a term call for the \nur\ inductive, the execution
either fails or succeeds with no alternatives left to explore.
% Below, we define \texttt{is\_det}.

% \input{tex_code/is_detS.tex}
\begin{definition}[\logo{}is\_det]\label{th:isdetS} $\lambda\ p\ \sigma\ t,\ \forall\ r\ v,\ t \cup \sigma \subseteq v \to$\\
  $\phantom{xxx} \nur\ p\ v\ [\sigma,\ [t , []]]\ r \to r = \mathrm{None} \lor \exists\ \sigma ,\ r = \mathrm{Some}\ ( \sigma ,\ \mnil ) $
\end{definition}

% denotes the function that tests whether the head of the term refers to a rigid
% constant with a deterministic type.

Due to the inadequacy of the stack semantics to reason about cut, we
state the main theorem in terms of the tree semantics and transfer it back
using the equivalence proof from~\cref{sec:equiv}.

We establish
an invariant characterizing deterministic computations.
% the previous lemma as it is stated is not an easy task. We need therefore to
% prove a lemma talking about determinacy declined on trees, and then reuse the
% theorems from \cref{sec:equiv} to derive a proof of determinacy for stacks.

\begin{definition}[\logo{}det\_tree]\label{th:isdetS} 
  The det\_tree predicate, shown in~\cref{fig:dettree}, identifies trees
  whose interpretation does not generate choice points. 
\end{definition}

\begin{figure}
\begin{coqcode}
  Fixpoint det_tree (p: program) A :=
    match A with
    | Unexplored cut => true
    | Unexplored (call t) => tm_is_det p t
    | Bot | Top => true
    | And A B0 B =>
        det_tree p B &&
        ((prune (success A) A == None) ||
         ((det_tree p A || (has_cut B && has_cuts B0)) &&
          det_trees p B0))
    | Or None _ B => det_tree p B
    | Or (Some A) _ B =>
        det_tree p A &&
        if has_cut A then det_tree p B else (B == Bot)
    end.
\end{coqcode}
\caption{The \emph{det\_tree} predicate\label{fig:dettree}}
\end{figure}

For a call to a term $t$, the tree is deterministic whenever $t$ calls a
deterministic predicate. The recursive cases correspond to the \andC and
\orC nodes.

A conjunction behaves deterministically whenever its right-hand side is a
deterministic tree. Two cases must then be considered.

If $\mathrm{prune}\ (\mathrm{success}\ A)\ A = \mathrm{None}$, then $A$ produces
no choice points: it either always fails or contains a single successful
execution path. Since $\mathrm{det\_tree}\ p\ B$ holds, the entire conjunction
is therefore deterministic.

Otherwise, the left-hand side may introduce backtracking, potentially
resuming the reset point, which must itself behave deterministically.
In this case, either $A$ is deterministic, or both $B$ and the reset
point contain a cut that prevents the non-deterministic behaviour of
$A$ from arising.

For \orC trees, the interesting case is when the left-hand side is
present. In this case, $A$ must be deterministic. If $A$
contains a superficial cut, then $B$ must also be deterministic. Indeed,
if $A$ succeeds, the cut discards $B$; otherwise, if $A$ fails before
reaching the cut, then no solution is produced by $A$ and $B$ is evaluated.

Finally, if $A$ does not contain a cut, then $B$ must be equal to
$\bot$, that is, a tree already discarded by a cut. This
condition is essential. Otherwise, if $A$ succeeds without a
superficial cut and $B$ is not $\bot$, then $B$ could still
produce a successful branch, leading to non-deterministic behaviour.

% \Cref{th:detchecktree} is proved by showing that deterministic trees are
% preserved by the operations \step and \na, and are used to prove
% \cref{th:detchecktree}.

\begin{definition}[\logo{}det\_checked]\label{def:detchecked}
  A program is det\_checked if all its rules satisfy
mutual exclusion and rule-body checking as per~\cite{fissore2026}. %, and \emph{tm\_is\_det}
\end{definition}

% \input{tex_code/det_tree_step.tex}
\begin{lemma}[\logo{}det\_tree\_step]\label{th:dettreestep}
  $\forall\ \mathrm{p}\ v\ \sigma\ t\ r,\ t\ \cup\ \sigma \subseteq v \to$\\
	$\phantom{xxx} \mathrm{det\_checked}\ \mathrm{p} \land \mathrm{step}\ u\ \mathrm{p}\ v\ \sigma\ t = r \to \mathrm{det\_tree}\ \mathrm{p}\ (\mathrm{snd}\ r) $
\end{lemma}

% \input{tex_code/det_tree_prune.tex}
\begin{lemma}[\logo{}det\_tree\_prune]\label{th:dettreeprune} $\forall\ p\ t\ t'\ b ,$\\
  $\phantom{xxx} \mathrm{det\_tree}\ p\ t \land \mathrm{prune}\ b\ t = \mathrm{Some}\ t' \to \mathrm{det\_tree}\ p\ t' $
\end{lemma}

% \input{tex_code/det_check_tree.tex}
\begin{lemma}[\logo{}det\_check\_tree]\label{th:detchecktree} $\forall\ \sigma\ v\ p\ t\ r\ b\ v',$\\
  $\phantom{xxx} t\ \cup\ \sigma \subseteq v \land \mathrm{det\_checked}\ p \land \mathrm{det\_tree}\ p\ t \to$\\
	$\phantom{xxx}\phantom{xxx}\run\ p\ v\ \sigma\ t\ r\ b\ v' \to r = \mathrm{Zero} \lor \exists\ \sigma ,\ r = ( \mathrm{One}\ \sigma )$
\end{lemma}
\begin{proof}
By induction on \run using the fact that \step and \na
preserve det\_tree~\cref{th:dettreestep,th:dettreeprune}.
\end{proof}

\begin{definition}[\logo{}tm\_is\_det]\label{def:tmisdet}
  Given a program $p$ and a term $t$,
  we say that $t$ is deterministic if its head is a predicate $f$
  which is declared as deterministic in $p$.
\end{definition}

% \input{tex_code/det_check_callS.tex}
\begin{theorem}[\logo{}det\_check\_call]\label{th:detcheckcall}$\forall\ p\ \sigma\ t, $\\
  % \vspace{.8em}\\
	$\phantom{xxx} \mathrm{det\_checked}\ p \land \mathrm{tm\_is\_det}\ p\ t \to \mathrm{is\_det}\ p\ \sigma\ (\mathrm{call}\ t)$
\end{theorem}
\begin{proof}
From \cref{th:detchecktree} we conclude that a call to a deterministic predicate
behaves deterministically on trees; then, thanks to \cref{th:runStorunTM1}, we derive
that it also behaves deterministically when executed in \nur.
\end{proof}

% \noindent Finally, the following lemma entails det\_check\_call:

% As an important remark, proving the same lemma with the stack semantics, it is
% difficult to correctly define the predicate \cI{det_stack}, checking for the
% deterministic behavior of a generic stack, since, similar to \cref{sec:run-prop},
% the lack of structure make this goal hard. We need therefore an intermediate
% data structure, like the tree to encompass this problem.

\section{Related work}

The only mechanization of \prologP semantics we are aware of is
that of Pusch in \isabelleP~\cite{10.1007/BFb0105415}, which, like
ours, follows Debray and Mishra~\cite[Sec.~4.3]{Debray1988}. Pusch
starts from this semantics and refines it by introducing
optimizations commonly found in the Warren abstract
machine~\cite{warren1983abstract,1991ait-wam}. Unlike our case study,
she does not use the semantics to prove properties of program
execution, and therefore does not encounter the difficulty described
in \cref{stack-sem} that led us to develop a semantics in which the
effect of cut is more explicit. In this sense, the two developments
are complementary: taken together, they suggest that our tree-based
semantics is equivalent to an optimized stack-based one.

Another relevant work is due to Kriener and King~\cite{2011king},
although we could not find their \rocqP{} source code. Their
semantics operates on programs in super-homogeneous form, 
which would have sufficed for the
first-order fragment considered in this paper but cannot easily
accommodate all of the features covered in~\cite{fissore2026},
in particular dynamic predicates. More
importantly, their mechanization does not relate the denotational
semantics to an operational semantics of the kind underlying standard
\prologP abstract machines, a link that our equivalence proof
provides.

Hanus and Prott~\cite{hanus2025} formalize in \rocqP{} a determinacy
checker for the logic-functional language \curryP. In this language,
choice points are explicitly denoted by the \cI{?} node in the syntax
of expressions: the overlap between rules is made explicit in
the syntax via a program transformation~\cite{progr_transformation}.
Similarly, the operator that absorbs choice points,
\cI{allValues}, explicitly takes as an argument the expression whose
choice points it discards, which makes the scope of this control
operator trivial to determine. Cut, by contrast, has no such syntactic
marker: its scope is implicit in the surrounding stack or tree
structure, which is precisely why relating the two semantics is
nontrivial in our setting.

The proof technique we use to relate the two semantics is inspired
by Bertot's mechanization of compiler correctness~\cite{yves-equiv}.
He relates the semantics of an imperative language to that of its
compiled assembly code and proves their equivalence. His approach uses
the compiler as the translation function from one language to the
other but does not introduce a decompiler, just as we do not define a
function from stacks to trees. To show that the assembly code behaves
like the imperative language, he proceeds by induction on the
execution of the assembly program; we adopt the same strategy in the
proof of \cref{th:runStorunT}.

For the static analysis of determinacy, we use a semantics in which
input arguments are \emph{matched}, rather than unified, against rule
heads. This choice agrees with the \elpiP interpreter, which is in
turn inspired by \bprologP's ``matching clauses''~\cite{1991ait-wam,zhou2012}.
Our results can nonetheless be applied to a semantics based entirely
on unification, by adding two assumptions to
\cref{th:detcheckcall}: the initial query must have ground inputs, and
the program must be well-moded. Since well-moded predicates produce
ground outputs from ground inputs, a well-moded program run from a
query with ground inputs behaves exactly as if its input arguments
were matched. In other words, \cref{th:matchingdisj} could be
reformulated in terms of unification at the cost of this ground-input
assumption, under which unification and matching coincide.

\section{Conclusion}

This paper presents two mechanized operational semantics for \prologP
with cut, together with a proof of their equivalence in \rocqP. The first
semantics, due to Debray and Mishra, is based on a stack of
alternatives: it is close to actual \prologP implementations,
including the first-order fragment of \elpiP, but it leaves the scope
of cut implicit in the shape of the stack, which makes it inconvenient
for reasoning about cut. The second semantics is novel and is based on
And--Or trees, in which the scope of cut is explicit in the tree
structure; it is well suited both to graphical illustrations of the
effect of cut and to stating introduction and elimination lemmas for
disjunction in its presence. We show how to translate trees into
stacks and use this translation to prove the two semantics equivalent.
Building on the tree-based semantics, we mechanize a soundness proof
for the first-order fragment of the determinacy checker
of~\cite{fissore2026}: a call to a predicate identified as
deterministic returns at most one solution.

The mechanization comprises approximately 2,500 lines of
specifications and 5,000 lines of \ssreflect{} proofs, built on the
Mathematical Components library~\cite{assia_mahboubi_2022_7118596} and
its finmap~\cite{finmap} extension. Roughly 30\% of the proof code is
devoted to the tree semantics, in particular the proofs in
\cref{sec:runT}; 15\% to the stack semantics; 35\% to the equivalence
proof; and the remaining 20\% to the determinacy checker. The
mechanization took us approximately $8$ months. We believe that the
\ssreflect{} proof language, and the proof style it encourages, made
it easier to carry out the many refactorings the development
underwent. The finmap library, in particular, provides comprehensive
support for reasoning about finite maps and finite sets over a
countable domain; it helped us express conditions such as the
disjointness between a set of variables and the domain of a
substitution, and to define the union of disjoint substitutions.

To keep the mechanization axiom-free, we implemented the first-order
unification algorithm ourselves, since we could not find an existing
\rocqP{} mechanization; a detailed account of this development is
beyond the scope of this paper. It amounts to about 1,700 lines of
\ssreflect{} code, including 200 lines for a termination proof based
on a lexicographic ordering and 800 lines for correctness and
completeness. Because \cref{th:unifren,th:matchingdisj} only involve
matching and unification of terms with disjoint support, we did not
need to prove that the algorithm produces most general unifiers.

Fully modeling the \elpiP language, and thereby mechanizing
\cite{fissore2026} in its entirety, requires accounting for
higher-order variables, i.e., variables that stand for predicates.
This extension is ongoing work, largely orthogonal to the present
paper. Its main difficulty is that substitutions may then bind
variables to predicates, whose signatures must remain compatible with
those assumed statically, in the sense of the $\subseteq$ relation
defined in~\cite[Section~5]{fissore2026}; accommodating dynamic
predicates also requires minor changes to the mutual
exclusion conditions.

\section{Use of artificial intelligence}

The research and the mechanization were carried out without resorting to AI
tools. The English prose was improved with the help of free LLMs.

\begin{acks}
  Acknowledgments withheld for double-blind review.
% We thank
% Yves Bertot and Laurent Thery for insightful
% discussions on how to prove the quivalence of the
% two semantics.\\
% ~\\
% Ce travail a bénéficié d'une aide du gouvernement
% français, gérée par l'Agence Nationale de la Recherche au titre du Pl­an
% d’investissement France 2030, dans le cadre du projet "UCA DS4H" portant la
% référence n° ANR-17-EURE-0004.
\end{acks}


\bibliographystyle{ACM-Reference-Format}

\bibliography{bib}

 \newpage

% \begin{appendix}

% \section{The step procedure (file tree.v, line 200)}

% \begin{coqcode}
% Fixpoint step p v s A :=
%   match A with
%   | Top                 => (v, Success, Top)
%   | Bot                 => (v, Failed, A)
%   | Unexplored cut      => (v, CutBrothers, Bot)
%   | Unexplored (call t) => 
%       let: (v, l) := backchain u p v t s in
%       (v, Expanded,
%       if l is ((s, r) :: xs) then Or None s (big_or r xs) else Bot)
%   | Or A sB B =>
%       if A is Some A then 
%         let: (v, tA, rA) := step p v s A in
%         (v, if is_cb tA then Expanded else tA, 
%           Or (Some rA) sB (if is_cb tA then Bot else B))
%       else
%         let: (v, tB, rB) := step p v sB B in
%         (v, if is_cb tB then Expanded else tB, Or A sB rB)
%   | And A B0 B =>
%       if success A then 
%         let: (v, tB, rB) := step p v (next_subst s A) B in
%         (v, tB, And (if is_cb tB then cutl A else A) B0 rB)
%       else
%         let: (v, tA, rA) := step p v s A in (v, tA, And rA B0 B)
%   end.
% \end{coqcode}

% \section{The prune procedure (file tree.v, line 243)}

%   \begin{coqcode}
%   Definition omap f o := match o with Some b => Some (f b) | _ => o end.
%   Fixpoint prune b A : option tree :=
%     match A with
%     | Bot => None
%     | Top => if b then None else Some Top
%     | Unexplored _ => Some A
%     | And A B0 B =>
%         let build_B0 b := omap (~$\lambda$~x. And x B0 (big_and B0)) (prune b A) in
%         if success A then
%           if prune b B is Some B' then Some (And A B0 B')
%           else build_B0 true
%         else if failed A then build_B0 false
%         else Some (And A B0 B)
%     | Or None sB B => omap (~$\lambda$~x. Or None sB x) (prune b B)
%     | Or (Some A) sB B =>
%         if prune b A is Some A' then Some (Or (Some A') sB B)
%         else omap (~$\lambda$~x. Or None sB x) (prune false B)
%   end.
%   \end{coqcode}


% \end{appendix}

\end{document}
